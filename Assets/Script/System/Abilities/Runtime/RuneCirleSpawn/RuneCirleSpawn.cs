public class RuneCirleSpawn : SpawnMono
{
    private SpawnMono summonPrefab;
    public override Launch(Vector2 target, float lifetime,
                             AbilityContext context, Action<object[]> execute)
    {

    }
}