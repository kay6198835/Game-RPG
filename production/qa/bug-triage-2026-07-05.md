# Bug Triage Report

> **Date**: 2026-07-05
> **Mode**: sprint (Sprint 3, 2026-06-23 to 2026-06-27 — formal sprint closed; post-sprint work continued through 2026-07-04)
> **Generated by**: /weekly-wrapup (automated Saturday — ran 8 days late)
> **Branch reviewed**: sprint-03 @ e50643c (merged into sprint-04)
> **CS files reviewed**: PlayerInputHandle.cs, PlayerState.cs, PlayerAttackState.cs, WeaponMelee.cs, Weapon.cs, RoomCell.cs, StatsSO.cs, Stat.cs, StatModifier.cs, StatType.cs, DerivedStatFormula.cs, EntityIdleState.cs, NegativeReciver.cs
> **Note**: Sprint-03 had two activity windows: formal sprint (2026-06-23 to 2026-06-27, 0 code commits) and post-sprint burst (2026-06-30 to 2026-07-04, significant code landed). Triage covers both.

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix this sprint (carry to Sprint 4) | 7 | 1 resolved since last triage (BUG-RC-1 CLOSED); 7 remain |
| P2 — Fix soon | 7 | 4 from prior triage + 3 new findings in StatSystem |
| P3 — Backlog | 5 | 4 unchanged + 1 new |
| P4 — Won't fix | 0 | — |

**Critical unfixed count**: 7 P1 bugs
**Fixed since last triage (2026-06-20)**: 1 (BUG-RC-1 CLOSED)
**New bugs found this triage**: 4 (P2: 3 + P3: 1)

---

## P1 Bugs — Carry to Sprint 4

| ID | System | Severity | Summary | Status |
|----|--------|----------|---------|--------|
| BUG-04 | Combat | S1 | `WeaponMelee.Attack()` foreach body empty — no `INegativeReceiver.TakeDamage()` call (`WeaponMelee.cs:29-32`) | UNCHANGED — confirmed still empty |
| BUG-09 | Animation | S1 (Critical) | `AnimationPlayerController.OnEnable()` line 21 registers `StartAnimation` twice; `EndAnimation` never fires | UNCHANGED — not touched in sprint-03 |
| BUG-06 | Game Loop | S1 | `Core.TakeDamage()` has no death check, no `ON_PLAYER_DEATH` emit | UNCHANGED |
| BUG-05/07/08 | Enemy AI | S1 | Death chain cluster: `EntityMoveState` null-deref before guard; `EntityDeathState` wrong base class; `EntityBasicState` death block empty | UNCHANGED |
| BUG-AH-1 | AbilityHolder | S1 (Build-blocking) | `using UnityEditor.Experimental.GraphView` in `AbilityHolder.cs` — breaks Player builds | UNCHANGED — confirmed still present |
| BUG-CORE-1 | Core | S1 | `GetCoreComponent<T>()` still uses `OfType<T>().FirstOrDefault()` LINQ — PlayerState.cs:40-44 calls it 5× per state entry | UNCHANGED — confirmed via PlayerState.cs read |
| BUG-PIH-1 | Input | S1 | `Invoke(nameof(ChangeIsTakeDamage), 0.2f)` at `PlayerInputHandle.cs:264` has no `CancelInvoke()` pair | UNCHANGED — confirmed still present |

---

## Resolved Since Last Triage

| ID | System | Resolution | Commit |
|----|--------|------------|--------|
| BUG-RC-1 | Map/Room | `RoomCell.GetDoor()` now returns `null` (not `new DoorController()`) when direction not found; doors created via `Instantiate(doorPrefab)` in `Setting()` | 05b76cc (2026-07-01) |

---

## P2 Bugs — Active

| ID | System | File | Summary | Sprint |
|----|--------|------|---------|--------|
| BUG-PIH-2 | Input | `PlayerInputHandle.cs:158` | `Camera.main.ScreenToWorldPoint(Input.mousePosition)` — mixes legacy `Input` with New Input System callback; `Camera.main` can be null | S4 |
| BUG-WM-1 | Combat | `WeaponMelee.cs:53,77` | `Debug.Log()` calls not guarded by `#if UNITY_EDITOR` — string alloc every attack check and every `SetAnimation()` call | S4 |
| BUG-PA-1 | Combat | `PlayerAttackState.cs:42,51,66` | Multiple `Debug.Log()` calls without `#if UNITY_EDITOR` guard — allocates strings every frame in combat states | S4 |
| BUG-SS-1 (NEW) | StatSystem | `StatsSO.cs:26-29` | `Start()` method on a ScriptableObject is never called by Unity — dead code; lazy `Initialize()` guard already handles this, but `Start()` is misleading | S4 |
| BUG-SS-2 (NEW) | StatSystem | `StatsSO.cs:15` | `private readonly Dictionary<StatType, Stat> stats` — shared SO instance will accumulate runtime modifiers across sessions/scenes; `initialized` flag means it never resets. Shared SOs must not carry mutable runtime state | S4 |
| BUG-WM-2 (NEW) | Combat | `WeaponMelee.cs:73,83` | `lastClickTime = Time.time` assigned twice in `SetAnimation()` — redundant double assignment | S4 |
| BUG-WM-3 (NEW) | Combat | `WeaponMelee.cs:86-99` | `DurationNextAttack()` returns 0 when no overrideClip is set — `deplayTime = 0` enables zero-cooldown attack spam | S4 |
| BUG-CORE-2 | Core | `Core.cs:8` | `[SerializeField]` on auto-property `Player` silently ignored | S4 |

---

## P3 Bugs — Backlog (unchanged unless noted)

| ID | System | Summary | Disposition |
|----|--------|---------|-------------|
| BUG-PIH-3 (NEW) | Input | `PlayerInputHandle.cs:7` — `starTime` field (typo for `startTime`) declared but never used | Backlog |
| BUG-RC-2 | Map/Room | `[SerializeField]` on auto-properties silently ignored | Backlog |
| BUG-DC-2 | Map/Door | `DoorController.CheckCanBeOpened()` dead method | Backlog |
| BUG-DC-3 | Map/Door | `DoorController.string Name` shadows `Object.name` | Backlog |
| BUG-RC-3 | Map/Room | `[ExecuteInEditMode]` + `Instantiate` in `Setting()` | Backlog |

---

## Code Review Findings — Sprint 3 CS Diff

### Critical / High

| Severity | File | Finding |
|----------|------|---------|
| CRITICAL | `WeaponMelee.cs:29-32` | BUG-04 CONFIRMED OPEN — `Attack()` foreach body completely empty; no damage applied on any melee hit |
| HIGH | `StatsSO.cs:15` | BUG-SS-2 NEW — mutable `Dictionary` runtime state on a shared SO; will not reset between runs without explicit clear |
| HIGH | `WeaponMelee.cs:86-99` | BUG-WM-3 NEW — `DurationNextAttack()` returns 0 when no overrideClip; deplayTime=0 → zero-cooldown attack |
| HIGH | `PlayerState.cs:40-44` | BUG-CORE-1 CONFIRMED — 5 LINQ `GetCoreComponent()` calls per state entry |

### Medium

| Severity | File | Finding |
|----------|------|---------|
| MEDIUM | `StatsSO.cs:26-29` | BUG-SS-1 NEW — `Start()` on SO is dead code |
| MEDIUM | `WeaponMelee.cs:53,77` | Debug.Log without `#if UNITY_EDITOR` guard — string alloc on every attack |
| MEDIUM | `PlayerAttackState.cs:42,51,66` | Three Debug.Log without `#if UNITY_EDITOR` guard — runs in LogicUpdate state loop |
| MEDIUM | `WeaponMelee.cs:73,83` | Double `lastClickTime` assignment — redundant |
| MEDIUM | `PlayerInputHandle.cs:158` | BUG-PIH-2 CONFIRMED — `Input.mousePosition` in New Input System callback |
| MEDIUM | `PlayerInputHandle.cs:264` | BUG-PIH-1 CONFIRMED — `Invoke()` without `CancelInvoke()` pairing |

### Low / Positive

| Severity | File | Finding |
|----------|------|---------|
| LOW | `PlayerAttackState.cs:28-31,56` | Commented-out code blocks — stale dead code |
| LOW | `WeaponMelee.cs:104` | `// need check late` stale TODO |
| LOW | `Weapon.cs:42-56` | `SetWeaponHolder()` body entirely commented out |
| LOW | `WeaponMelee.cs:2` | `using Unity.VisualScripting` — unused import |
| LOW | `PlayerInputHandle.cs:7` | `starTime` field (typo, never used) |
| POSITIVE | `RoomCell.cs:60-68` | `GetDoor()` now returns `null` correctly — BUG-RC-1 closed |
| POSITIVE | `StatsSO.cs` | Clean SO API: `Get()`, `AddModifier()`, `RemoveModifiersFromSource()`, `AddPrimaryPoint()` — follows SO-first pattern; event-driven with `OnStatChanged` |
| POSITIVE | `Stat.cs / StatModifier.cs` | Modifier stack pattern with source tracking — enables clean remove-by-source semantics |
| POSITIVE | `PlayerInputHandle.cs:101-153` | `OnEnable`/`OnDisable` input callback symmetry is correct and complete |
| POSITIVE | `PlayerAttackState.cs:32-61` | `StatusAnimation` enum switch drives combo logic cleanly |

---

## New Architecture Alert — StatSystem and Skill Enhance

Two new systems landed as post-sprint work on sprint-03 (merged from parallel branches):

### StatSystem (`Assets/Script/StatSystem/`)
- `StatType.cs` — enum for Primary (STR/DEX/INT/VIT/LUK) and Derived stats
- `Stat.cs` — value + modifier stack with source tracking
- `StatModifier.cs` — flat/percent-add/percent-mult modifier types
- `DerivedStatFormula.cs` — SO-based formula (maps primary stats → derived stats per level)
- `StatsSO.cs` — ScriptableObject profile; event-driven `OnStatChanged`; lazy-initialize pattern

**Critical issue**: `StatsSO` holds mutable `Dictionary<StatType, Stat>` runtime state. As a shared SO, this state persists across sessions. Must add `OnEnable()` / `OnDisable()` reset or use instanced SOs per-character. See BUG-SS-2.

### Skill Enhance (`Assets/Skill Enhance/Scripts/Abilities/`)
- Data-driven ability system: `AbilityDefinition` SO → `AbilityInstance` runtime → `AbilitySystem` host
- Conditions (`HasEnoughManaCondition`, `NotDeadCondition`) and Effects (`DamageInFrontEffect`, `LungeForwardEffect`, `ShootSpiritOrbEffect`) are separate, composable objects
- Not yet integrated with existing `ActivateSkill` / `AbilityHolder` pipeline

**Integration path needed in Sprint 4**: decide whether to adopt `Skill Enhance` as replacement for `ActivateSkill` pipeline or keep both. Decision should be an Architecture Decision Record (ADR).

> **Resolved 2026-08-22** (appended by documentation audit; original text above unchanged). The
> decision asked for here went unmade for seven weeks. Owner decision: **keep, do not adopt** — the
> framework was moved out of `Assets/` to `prototypes/skill-enhance-abilities/`, so Unity no longer
> compiles it and `ActivateSkill` remains the only ability pipeline. No ADR was written: with the
> code parked outside the build there is no live architectural choice left to record. Rationale and
> revival steps are in that directory's `README.md`.

---

## Trend Analysis

**Volume**: 18 open bugs (P1: 7, P2: 7, P3: 5) — net +1 vs last triage (1 resolved, 4 new)
**Hot spot**: Player combat system — 9 of 18 active bugs
**Regressions**: 0 against previously completed stories
**Aged bugs (>3 sprints)**: BUG-04, BUG-09, BUG-06, BUG-05/07/08 — 8th consecutive sprint unresolved
**Post-sprint burst**: significant code activity 2026-06-30 to 2026-07-04 outside the formal sprint window

---

## Recommended Actions for Sprint 4

Priority order:

1. **Fix BUG-AH-1** — `AbilityHolder.cs`: remove `using UnityEditor.Experimental.GraphView` (2-line fix, blocks every Player build)
2. **Fix BUG-09** — `AnimationPlayerController.cs` lines 21 + 29: `StartAnimation` → `EndAnimation` (2-line fix, unblocks all state exits)
3. **Fix BUG-CORE-1** — `Core.GetCoreComponent<T>()`: replace LINQ with `foreach`
4. **Fix BUG-04** — `WeaponMelee.Attack()`: add `INegativeReceiver.TakeDamage(currrentSA.attackDamege, transform.position)` inside foreach (1-line fix, enables first real combat damage)
5. **Fix BUG-PIH-1** — `PlayerInputHandle.cs:264`: add `CancelInvoke(nameof(ChangeIsTakeDamage))` before `Invoke()`
6. **Fix BUG-SS-2** — `StatsSO`: make runtime dictionary instance-per-character (not shared SO), or add `OnEnable` reset
7. **Architecture decision** — write ADR: adopt `Skill Enhance` ability pipeline vs. extend `ActivateSkill` — unblock integration path
8. **Fix BUG-WM-3** — `DurationNextAttack()`: guard zero-totalDuration to prevent zero-cooldown attack
