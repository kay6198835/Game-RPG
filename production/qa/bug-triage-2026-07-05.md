# Bug Triage Report

> **Date**: 2026-07-05
> **Mode**: sprint (Sprint 3, 2026-06-23 to 2026-06-27 — post-sprint wrap-up; wrap-up delayed to 2026-07-05)
> **Generated by**: /weekly-wrapup (automated Saturday)
> **Branch reviewed**: sprint-03 (HEAD ea9c534)
> **Files reviewed**: 14 changed `.cs` files since last wrap-up (2026-06-20 commit 21744a4)
> **New code landed**: Stats system (StatsSO.cs, Stat.cs, StatType.cs, StatModifier.cs, DerivedStatFormula.cs), NegativeReciver.cs stub, RoomCell.cs fix, EntityIdleState.cs minor, PlayerAttackState.cs combo logic, PlayerState.cs StatusAnimation, WeaponMelee.cs SetAnimation, Weapon.cs.

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix immediately (block compile or gameplay) | 9 | 6 carried from Sprint 2 + 3 new from stats system |
| P2 — Fix this sprint | 5 | Code-review findings: architecture violations |
| P3 — Backlog | 3 | Dead code, style |
| P4 — Won't fix | 0 | — |

**Critical unfixed count**: 9 P1 bugs  
**Fixed since last triage (2026-06-20)**: 1 (BUG-RC-1 — `RoomCell.GetDoor()` `new DoorController()` → replaced with prefab instantiation via commit 05b76cc)  
**New bugs found this triage**: 3 P1 (stats system compile errors + NegativeReciver crash), 2 P2

---

## P1 Bugs — Must Fix (Sprint 4)

| ID | System | Severity | Summary | Status |
|----|--------|----------|---------|--------|
| BUG-04 | Combat | S1 | `WeaponMelee.Attack()` foreach body empty — no `INegativeReceiver.TakeDamage()` call (`WeaponMelee.cs` ln 23–26) | **OPEN** — confirmed unchanged this sprint; `foreach` body still `{}` |
| BUG-09 | Animation | S1 | `AnimationPlayerController.OnEnable()` ln 21 registers `StartAnimation` twice; `EndAnimation` callback never fires — all combat states can get permanently stuck | **OPEN** — not touched this sprint |
| BUG-06 | Game Loop | S1 | `Core.TakeDamage()` no death check, no `ON_PLAYER_DEATH` emit; `EventID` enum missing `ON_PLAYER_DEATH` | **OPEN** |
| BUG-05/07/08 | Enemy AI | S1 | Death chain cluster: `EntityMoveState` null-deref before null guard; `EntityDeathState` wrong base class (`MonoBehaviour` not `EntityState`); `EntityBasicState` death block empty | **OPEN** |
| BUG-SS-1 | Stats System | S1 (Compile) | `StatsSO.cs` ln 36–37: references `StatTypes.Primary` and `StatTypes.Derived` — `StatTypes` static class does not exist anywhere in codebase → **compile error**, entire stats system unusable | **NEW — OPEN** |
| BUG-SS-2 | Stats System | S1 (Logic) | `StatsSO.Start()` (ln 26): `Start()` is a `MonoBehaviour` lifecycle method, NOT called on `ScriptableObject` instances — `Initialize()` is never invoked automatically; `stats` dictionary stays empty; all `Get()` calls return 0 | **NEW — OPEN** |
| BUG-NR-1 | Combat | S1 (Crash) | `NegativeReciver.cs` `TakeDamage()` throws `NotImplementedException()` — if any code calls `INegativeReceiver.TakeDamage()` on a player (e.g. enemy hit), the game crashes immediately | **NEW — OPEN** |
| BUG-AH-1 | Build | S1 (Build-break) | `AbilityHolder.cs` ln 4: `using UnityEditor.Experimental.GraphView;` — breaks Player builds; must be wrapped `#if UNITY_EDITOR` or removed | **OPEN — Carried Sprint 2→3→4** |
| (blocked) | Room Progression | S1 | Room-clear lock/unlock blocked on BUG-05/07/08 enemy death chain | **BLOCKED** |

---

## P2 Bugs — Fix This Sprint

| ID | System | Summary | File | Notes |
|----|--------|---------|------|-------|
| BUG-SS-3 | Stats System | `DerivedStatFormula.cs` is a plain C# class — cannot be authored as a Unity asset. `StatsSO.statFormulas` is `DerivedStatFormula[]` serialized inline, not a SO array. Formulas cannot be shared across multiple `StatsSO` profiles, and cannot be tuned without code. Design intent (data-driven) is not met. | `DerivedStatFormula.cs` | Needs `[CreateAssetMenu]` + `ScriptableObject` base, or redesign as `[Serializable]` struct explicitly acknowledged |
| BUG-SS-4 | Stats System | `Stat.cs` contains 5 distinct type definitions in a single file (`Stat`, `StatType`, `StatTypeExtensions`, `ModifierType`, `StatModifier`). Violates one-type-per-file convention; makes diffs unreadable; Unity may warn on mismatch between filename and primary class | `Stat.cs` | Split into `Stat.cs`, `StatType.cs` (already partially split), `StatModifier.cs` |
| BUG-PIH-1 | Input | `PlayerInputHandle` (`PlayerInputHandler`) — audit all `Invoke`/`InvokeRepeating` calls for `CancelInvoke` pairing in `OnDisable`/state exit to prevent leaked invocations | `PlayerInputHandle.cs` | Carried from Sprint 2 triage |
| BUG-S2-03 | Architecture | `Core.GetCoreComponent<T>()` still uses LINQ `OfType<T>().FirstOrDefault()` — allocates every call; violated zero-alloc rule since Sprint 2 | `Core.cs` | S3-03 carried Sprint 2→3→4 |
| BUG-WM-1 | Combat | `WeaponMelee.cs` has `using Unity.VisualScripting;` at top — VisualScripting is not used anywhere in this file; unnecessary dependency import | `WeaponMelee.cs` | Minor, remove import |

---

## P3 Bugs — Backlog

| ID | Summary | File |
|----|---------|------|
| BUG-DZ-1 | `Weapon.cs` `SetWeaponHolder()` method body is entirely commented out (10 lines) — method is dead code or incomplete stub | `Weapon.cs` |
| BUG-DZ-2 | `PlayerAttackState.cs` ln 28–30: commented-out `if (isAnimationFinishedTrigger && inputHandler.IsAttack)` block — stale dead code from old animation model; should be deleted | `PlayerAttackState.cs` |
| BUG-DZ-3 | `RoomCell.cs` has `using Unity.VisualScripting;` — not used in file | `RoomCell.cs` |

---

## Fixed Since Last Triage

| ID | Summary | Fix Commit |
|----|---------|------------|
| BUG-RC-1 | `RoomCell.GetDoor()` was constructing `new DoorController()` (MonoBehaviour via `new`) — now correctly uses prefab Instantiate | 05b76cc (2026-06-26 ish) |

---

## Carry-Over Into Sprint 4

**Must-Have fixes (Sprint 4 = Sprint 3 all-carry + new critical):**

| Priority Order | Bug | Est |
|----------------|-----|-----|
| 1 | BUG-SS-1 — create `StatTypes` static class with `Primary`/`Derived` enumerables | 0.25d |
| 2 | BUG-SS-2 — replace `Start()` with `OnEnable()` or call-site `Initialize()` pattern for ScriptableObjects | 0.25d |
| 3 | BUG-NR-1 — implement `NegativeReciver.TakeDamage()` (player component) instead of `throw` | 0.25d |
| 4 | BUG-AH-1 — remove/wrap `UnityEditor` import in `AbilityHolder.cs` | 0.25d |
| 5 | BUG-09 — fix `AnimationPlayerController` double-registration | 0.25d |
| 6 | BUG-S2-03 — `Core.GetCoreComponent<T>()` LINQ → foreach | 0.25d |
| 7 | BUG-04 — `WeaponMelee.Attack()` add `TakeDamage` call | 0.25d |
