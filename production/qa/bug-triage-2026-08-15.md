# Bug Triage Report

> **Date**: 2026-08-15
> **Mode**: sprint (`sprint-09` Saturday weekly wrap-up)
> **Generated by**: `pm-weekly-wrapup` scheduled task (autonomous)
> **Open items processed**: 30 (carried from `bug-triage-2026-08-10.md`, 2 closed this cycle, 0 new)
> **Sprint in scope**: sprint-09 (branch `sprint-09`, verified against `origin/sprint-09` tip
> `bfe7dd4`)
> **Diff reviewed**: `01476f4..bfe7dd4` (23 changed `.cs` files since last wrap-up commit
> `chore(wrapup): weekly wrap-up 2026-08-10`) — direct read of the weapon-architecture refactor
> (`Weapon.cs`, `MeleeWeapon.cs`, `RangeWeapon.cs`, `WeaponHolder.cs`, `PlayerAttackState.cs`,
> `EntityWeaponMelee.cs`, `EntityInput.cs`, pooling infra, stats SOs) plus targeted re-verification of
> every P1 item not touched by this diff (`EntityCore.cs`, `EntityNegativeReciver.cs`,
> `PlayerDeathState.cs`, `EnemySpawner.cs`).

> **Correction note (methodology, kept for the record):** this run's first pass was built against a
> stale local `sprint-09` git ref that had not been fetched from `origin` and was 8 commits behind —
> it produced an incorrect headline finding (a phantom compile error) based on an intermediate state
> that was never actually the branch tip. That pass was discarded before anything was pushed, once a
> `git push` rejection surfaced the staleness. This report is built from `origin/sprint-09`'s actual
> tip (`bfe7dd4`), fetched and confirmed directly. See Process Improvements in this cycle's retro for
> the prevention recommendation (fetch before branching, every time, not just on ambiguity).

---

## ⚠️ HEADLINE FINDING — the weapon-architecture refactor lands, closing BUG-041 and BUG-059 for real, but the sprint's largest P0 (BUG-042/053/054) and the process-gate item remain fully untouched

23 files changed on `sprint-09` this week, dominated by a real unify-melee-and-ranged refactor (commits
`797a562`, `6aba70c`, `f40a6e8`, `4241a3b`, merged via `7012d31`/`bfe7dd4` from
`claude/weapon-architecture-stats-dermi4`). Reading the resulting code directly (not the commit
messages):

- **BUG-041 (player deals zero melee damage): CLOSED.** `MeleeWeapon.OnActivate()`
  (`Weapons/MeleeWeapon/MeleeWeapon.cs:24-36`) now correctly runs
  `Physics2D.OverlapCircleNonAlloc(centerAttackPosition, currentStage.attackRange, hits, stats.LayerMask)`
  against a pre-allocated `Collider2D[]` buffer, then loops hits calling
  `INegativeReceiver.TakeDamage(currentStage.attackDamege, transform.position)` — matches
  `weapon-skill-code.md`'s contract and mirrors `EntityWeaponMelee.Attack()` as required. Call chain
  verified end-to-end: `PlayerAttackState.cs` case `StatusAnimation.OnActivate` →
  `weaponHolder.MakeDamage()` (`WeaponHolder.cs:38-41`) → `weapon.OnActivate()`. Not yet Play-Mode
  confirmed (S9-12 still blocked on no Unity CLI in this environment), but the static implementation is
  correct and complete — a real close, not another "no longer throws" false positive like BUG-042/053
  turned out to be twice already.
- **BUG-059 (RangeWeapon.Attack() empty, found last cycle): CLOSED as a side effect.**
  `RangeWeapon.OnActivate()` (`Weapons/RangeWeapon/RangeWeapon.cs:30-49`) now spawns pooled projectiles
  through the new `ObjectPoolManager`/`Pool` classes, with spread-angle fan-out for multi-projectile
  stages. Not previously a Must-Have item, but confirmed fixed.
- **BUG-032 (enemy skill NullRef, one-liner): CODE-LEVEL FIX APPLIED, prefab wiring unverifiable from
  text review.** `EntityWeaponMelee.Awake()` now guards `if (holder != null) { holder.Core.GetCoreComponent(out entityInput); aim = entityInput; }`
  (`EntityWeaponMelee.cs:16-20`). Since `EntityWeapon.holder` is `[SerializeField] protected
  EntityWeaponHolder holder`, Unity populates it from the prefab's Inspector wiring before `Awake()`
  runs — so this guard is structurally correct *if* the enemy prefab actually has the field wired. This
  triage cannot open `.prefab` assets to confirm that wiring; recommend S9-12's Play Mode session
  include an explicit check that `entityInput` is non-null on a live enemy, not just that the code
  compiles.
- **Legacy dead-code cleanup: `PlayerCombat.cs` (129 lines, CLAUDE.md-flagged "Legacy — do not use") and
  `WeaponsController.cs` (60 lines, BUG-060 from last cycle: live references to `PlayerCombat` with an
  unassigned `currWp` that would NullReferenceException on any call) were both deleted this week.**
  Directly resolves BUG-060's recommended action ("confirm unused, then delete"). `SwordAndShield.cs`
  and `WeaponSprite.cs` (both near-empty stubs) were also removed.
- **BUG-042 (`EntityCore.TakeDamage()` throws `NotImplementedException`): CONFIRMED STILL FULLY OPEN**,
  byte-identical to last cycle — this file was not touched by this week's diff.
- **BUG-053/BUG-054 (`EntityNegativeReciver.cs` duplicate, wrong-hub + uninitialized health): CONFIRMED
  STILL FULLY OPEN**, byte-identical — not touched this week either. This remains the sprint's single
  largest Must-Have item (S9-02, 0.3d) and has now received zero code movement across all 5 scheduled
  days plus the weekend.
- **BUG-033 (spawn null-guard order, one-liner): CONFIRMED STILL OPEN**, unchanged —
  `EnemySpawner.cs:62` still `set.Count == 0 || set == null`.
- **BUG-044 (`PlayerDeathState` orphaned): CONFIRMED STILL OPEN**, unchanged — `LogicUpdate()` body
  fully commented out.
- **Secondary finding (moderate confidence, new this cycle) — `PlayerInputHandle.cs`'s new
  `statusAnimation` mirror field is only ever written from `PlayerAttackState`'s `default:` switch
  branch** (`PlayerAttackState.cs:60-62`), which fires only for the two `StatusAnimation` values with no
  explicit `case` (`Animaing`, `StartRangeTrigger`). The four explicitly-handled cases (`OnActivate`,
  `OffActivate`, `EndRangeTrigger`, `End`) never call `SetStatusAnimation`, so the attack-buffer gate at
  `PlayerInputHandle.cs:199-202` (`StartRangeTrigger <= statusAnimation < EndRangeTrigger`) reads a
  cached value that cannot reflect the true phase once combat progresses past `StartRangeTrigger`. Not
  confirmed against live behavior (no Play Mode session available); recommend folding this into S9-12's
  verification pass rather than filing as a standalone bug pending that confirmation, since it may be
  benign in practice if the two adjacent phases share buffer semantics by design.

**Net: this is the first cycle in three (Sprint 7→8→9) to close real Must-Have code this week, and the
first time BUG-041 has moved past "still open" without a hidden second defect the way BUG-042/053 did
twice. The sprint's largest single item (BUG-042/053/054) and its process-gate item (S9-00) received
zero movement.**

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix next sprint (blocking) | 9 | BUG-041 and BUG-059 closed this cycle; BUG-032 downgraded to "code fix applied, needs Play Mode confirmation of prefab wiring"; 9 remain fully open |
| P2 — Fix soon | 8 | Unchanged from last cycle, minus BUG-059 (closed, moved out of P2) |
| P3 — Backlog | 8 | Unchanged, minus BUG-060 (closed via deletion, moved out of P3) |
| P4 — Decision-avoidance, not a code bug | 1 | S4-05/S4-06, now 10th carry with zero movement |

**Critical (S1) unfixed count**: 9 (down from 11 — first real reduction in 3 cycles, not a paper one)

---

## P1 Bugs — Fix Before Anything Else Ships

| ID | System | Severity | Summary | File:Line |
|----|--------|----------|---------|-----------|
| ~~BUG-041~~ | Weapon | — | **CLOSED this cycle** — `MeleeWeapon.OnActivate()` now correctly resolves and applies damage. Static-verified only; Play Mode confirmation still pending (S9-12). | `Weapons/MeleeWeapon/MeleeWeapon.cs:24-36` |
| BUG-042 | Enemy AI | S1 | `EntityCore.TakeDamage()` still `throw new System.NotImplementedException()`, unchanged, 5th carry | `Entity/Core/EntityCore.cs:9-12` |
| BUG-053 | Enemy AI | S1 | `EntityNegativeReciver.TakeDamage()` still calls `Core.GetCoreComponent(out PlayerInputHandler input)` (wrong hub) and still emits `ON_PLAYER_DEATH` on enemy death, unchanged | `Entity/CoreComponent/EntityNegativeReciver.cs:10,13` |
| BUG-054 | Enemy AI | S1 | `EntityNegativeReciver.currentHealth` still a never-initialized `public int`, defaults to `0` — `TakeDamage()` still a silent no-op, unchanged | `Entity/CoreComponent/EntityNegativeReciver.cs:5,8` |
| BUG-043 | Enemy AI | S1 | Two divergent enemy attack paths — not in this week's diff scope, carried unverified from last cycle | `Entity/CoreComponent/EntityAttack.cs` vs `Entity/EntityWeaponMelee.cs` |
| BUG-044 | Player | S1 | `PlayerDeathState.LogicUpdate()` body still fully commented out — re-verified directly this cycle, unchanged | `Player/States/PlayerDeathState.cs:17-24` |
| Bug #6 (S7-11, carried, 10th cycle) | Player damage | S1 | `NegativeReciver.currentHealth` still disconnected from `PlayerData.currentHealth` — not in scope this cycle, carried unverified | `Player/CoreComponent/NegativeReciver.cs` |
| BUG-032 (S7-09, carried, 4th cycle) | Enemy AI | S1 → downgraded | Code-level fix applied this week (`EntityWeaponMelee.cs:16-20`, guarded `GetCoreComponent`) — leaving open pending Play Mode confirmation that the enemy prefab's `holder` reference is actually wired in the Inspector, which this triage cannot verify from text alone | `Entity/EntityWeaponMelee.cs:16-20` |
| BUG-033/BUG-ES-1 (S7-10, carried, 7th cycle) | Enemy spawn | S1 | `EnemySpawner.cs:62` null-check order still wrong — re-verified directly this cycle, unchanged | `Enemy/EnemySpawner.cs:62` |
| Bug #14 (carried) | Map | S1 | `MazeController.Awake()` still missing `return;` — not in scope this cycle, carried unverified | `Map/Maze/MazeController.cs:17-21` |
| Bug #15 (S7-D2, carried) | Map/Room | S1 | `File.ReadAllText(Application.dataPath + filePath)` still on the runtime path — not in scope this cycle, carried unverified | `Map/Room/RoomGeneraterController.cs` |

---

## P2 Bugs — Fix Soon

| ID | System | Severity | Summary | Target |
|----|--------|----------|---------|--------|
| ~~BUG-059~~ | Weapon | — | **CLOSED this cycle** — `RangeWeapon.OnActivate()` now spawns pooled projectiles with spread-angle support | `Weapons/RangeWeapon/RangeWeapon.cs:30-49` |
| BUG-055 | Enemy AI | S2 | `EntityBasicState.cs` death check vs. `EntityNegativeReciver`'s disconnected local health field — not in scope this cycle, carried unverified | `Entity/States/EntityBasicState.cs` |
| BUG-057 | Enemy AI | S2 | `EntityAttackState.LogicUpdate()` null-check gap — not in scope this cycle, carried unverified | `Entity/States/EntityAttackState.cs:24` |
| BUG-045 (carried) | Pathfinding | S2 (perf) | `AStar.cs` per-call allocation — not in scope, carried unverified | Sprint 10 |
| BUG-046 (carried) | Enemy AI | S2 | `EntityWeaponMelee.cs` still uses allocating `Physics2D.OverlapCircle`, not `OverlapCircleNonAlloc` — confirmed still present this cycle (`EntityWeaponMelee.cs:29`), unlike the player-side melee weapon which now correctly uses `OverlapCircleNonAlloc` after this week's refactor | `Entity/EntityWeaponMelee.cs:29` |
| BUG-047 (carried) | Enemy AI | S3 | `EntityMovement.cs` caches `EnemyManager.Instance.Grid` once, no null-check — not in scope, carried unverified | `Entity/CoreComponent/EntityMovement.cs:32` |
| BUG-035/036 (carried) | Pathfinding | S2 | Diagonal-neighbor/ordering issues — not in scope, carried unverified | Sprint 10 |
| Bug #12 (carried) | Map/LevelEdit | S2 | `LevelManager` singleton — not in scope, carried unverified | Sprint 10 |
| Bug #16 (carried) | Map/Room | S2 | `RoomType` enum unread at runtime — not in scope, carried unverified | Sprint 10 |

---

## P3/P4 Bugs — Backlog / Won't Fix

| ID | System | Severity | Summary | Disposition |
|----|--------|----------|---------|-------------|
| ~~BUG-060~~ | Weapon | — | **CLOSED this cycle** — `WeaponsController.cs` and its dead `PlayerCombat` references deleted entirely | Resolved |
| BUG-056 (carried) | Enemy AI | S3 | `EntityIdleState.cs` hardcoded `idleDurationTime = 3` overwrite — not in scope, carried unverified | Backlog |
| BUG-058 (carried) | Player | S3 | `WeaponHolder.Weapon` public field — **superseded**: `WeaponHolder.cs` was rewritten this week; the field is now `[SerializeField] private Weapon weapon` with a `public Weapon Weapon { get; }` property, matching `gameplay-code.md`. Half of BUG-058 (the `WeaponHolder` half) is fixed as a side effect; the other half (`Player.cs`'s redundant public `stateMachine` field) not re-verified this cycle. | Backlog — re-verify `Player.cs` half next cycle |
| BUG-061 (carried) | Map/Room | S4 | `RoomGeneraterController.cs` dead ternary — not in scope, carried unverified | Backlog |
| BUG-052 (carried) | Cross-cutting | S3 | CLAUDE.md coverage gaps — not in scope, carried unverified | Backlog |
| BUG-049 (carried) | Base/Core | S4 | `CoreBase.cs` no-op `[SerializeField]` — not in scope, carried unverified | Backlog |
| BUG-050 (carried) | Enemy AI | S4 | Raw `Debug.Log` calls — not in scope, carried unverified | Backlog |
| Bug #17 (carried) | Map/Room | S3 | `DoorController` dead/no-op methods — not in scope, carried unverified | Backlog |
| S4-05/S4-06 (P4, 10th carry) | Decision | — | Keep-or-cut call, zero movement across 10 cycles now | Owner must decide |

---

## Systemic Issues Flagged

1. **The largest single Must-Have item (BUG-042/053/054) has now received zero code movement across 4
   consecutive triage cycles.** Everything else in this sprint's Must-Have set either closed (BUG-041)
   or has a partial fix pending verification (BUG-032); this one item alone is the reason the sprint
   cannot close clean.
2. **S9-00, the process gate meant to catch off-branch/off-plan drift, was never drafted this sprint —
   4th carry.** Notably, this week's real refactor landed via a branch named
   `claude/weapon-architecture-stats-dermi4`, merged into `sprint-09` through two merge commits
   (`7012d31`, `bfe7dd4`) rather than direct commits — itself an instance of the pattern S9-00 exists to
   govern, just one that happened to land real, working code this time rather than drift/regress. The
   absence of a written rule means there's no way to distinguish "acceptable off-branch work, reconciled
   promptly" from "risky off-branch drift" except by re-reading the diff after the fact each cycle.
3. **`production/qa/bugs/` remains at 2 individual files** after 4+ cycles of this being flagged
   (S7-D3/S8-D1/S9-D1) — unchanged this cycle.
4. **This triage's own first-pass methodology error is worth naming plainly**: working from a
   not-freshly-fetched local branch ref produced an entirely fabricated headline finding before the
   `git push` rejection surfaced the staleness. See this cycle's retro, Process Improvements, for the
   fix.

---

## Trend Analysis

**Volume**: 30 open (30 carried, 2 closed, 0 new net) vs. 30 last cycle — first cycle with net closures
instead of net growth since at least Sprint 7.
**Hot spot**: shifting — `Assets/Script/Weapons/MeleeWeapon/` and `Assets/Script/Weapons/RangeWeapon/`
are now functionally sound (both BUG-041 and BUG-059 closed); `Assets/Script/Character/Entity/` remains
the hot spot, unchanged, still the second-longest-standing broken direction of the core combat loop.
**Regressions**: none confirmed this cycle.
**Aged bugs (>2 sprints old)**: Bug #6 (damage chain, ~sprint-04, 10th carry), Bug #12 (LevelManager
singleton, ~sprint-04), Bug #15 (JSON load, ~sprint-05), S4-05/S4-06 (10th carry, oldest unresolved
item) — all four unchanged and none touched by this week's refactor.
**First net-positive week since Sprint 6-era history visible in these reports**: 2 P1/P2 bugs closed
(BUG-041, BUG-059), plus BUG-060 closed via deletion and half of BUG-058 fixed as a refactor side
effect — real progress, concentrated entirely in the weapon-architecture area, with zero spillover into
the enemy-AI damage chain that remains this sprint's actual blocker.

---

## Recommended Actions

1. **S9-02 (BUG-042/053/054, bundled) is now the only remaining blocker to the sprint's core combat
   goal** — recommend this be the literal first item of Sprint 10, given everything else in this
   sprint's original Must-Have set is either closed or pending Play Mode confirmation only.
2. **Run S9-12's Play Mode verification pass covering three things, not just "no exceptions"**: (a)
   player melee damage actually lands (BUG-041's static fix), (b) the enemy prefab's `holder` reference
   is wired so `EntityWeaponMelee`'s `entityInput` is non-null (BUG-032's remaining uncertainty), and
   (c) the `statusAnimation` buffer-gate behaves correctly through a full combo (this cycle's secondary
   finding).
3. **Land S9-00 now** — this week is the clearest possible illustration of why it matters: a
   feature-branch merge landed real, correct work this time, but the sprint has no way to have known
   that in advance or distinguish it from a risky drift pattern except by re-reading the whole diff after
   the fact, again.
4. File individual `production/qa/bugs/BUG-NNN.md` reports for at least the 9-item P1 table — 4+ cycles
   recommended, still 2 files exist.
5. Force the S4-05/S4-06 decision — 10 cycles of silent carry.
