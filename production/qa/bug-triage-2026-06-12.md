# Bug Triage Report

> **Date**: 2026-06-12
> **Mode**: sprint
> **Generated by**: /bug-triage
> **Open bugs processed**: 13 (carried over) + 2 new = 15
> **Sprint in scope**: sprint-01 (2026-06-08 → 2026-06-12, day 5/5 — sprint window now closed)

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix this sprint | 6 | Identical to sprint-01 Must-Haves; **0 of 6 fixed during the sprint window** |
| P2 — Fix soon | 3 | 1 new this week (DeleteDoorTileMap), 2 carryover |
| P3 — Backlog | 5 | No gameplay impact |
| P4 — Won't fix | 0 | — |

**Critical (S1/S2) unfixed count**: 7 (6 carryover P1 + BUG-RGC-4)

**Likely resolved this/last week (needs QA confirmation)**: BUG-RGC-1, BUG-RGC-2, BUG-RGC-3 — superseded by the room-loading rewrite (`b2fb184 add dotweet, fix logic reload old map`). Recommend a quick `/smoke-check` to confirm before closing.

---

## P1 Bugs — Fix This Sprint (carried into sprint-02)

| ID | System | Severity | Summary | Assigned to | Story |
|----|--------|----------|---------|-------------|-------|
| BUG-04 | Combat | S1 | `WeaponMelee.Attack()` foreach body empty — player attacks deal 0 damage | sprint-02 | S1-01 (carryover) |
| BUG-09 | Animation | S2 | `AnimationPlayerController.OnEnable` registers `StartAnimation` twice — `EndAnimation` never fires, combat states get permanently stuck | sprint-02 | S1-02 (carryover) |
| BUG-06 | Game Loop | S1 | No player death check in `Core.TakeDamage()` — health goes negative, no `ON_PLAYER_DEATH`, no restart | sprint-02 | S1-03 (carryover) |
| BUG-05 | Enemy AI | S2 | `EntityMoveState.LogicUpdate()` dereferences `Target.transform.position` (line 30) before the null check (line 34) — NullRef on target loss | sprint-02 | S1-04 (carryover) |
| BUG-07 | Enemy AI | S2 | `EntityDeathState` still extends `MonoBehaviour` — not wired into `EntityStateMachine` | sprint-02 | S1-04 (carryover) |
| BUG-08 | Enemy AI | S2 | `EntityBasicState.LogicUpdate()` `Health <= 0` block still empty (no transition to death state, no `ON_CLEAR_ENEMY`) | sprint-02 | S1-04 (carryover) |

---

## P2 Bugs — Fix Soon

| ID | System | Severity | Summary | Target Sprint |
|----|--------|----------|---------|---------------|
| BUG-RGC-4 | Map/Room | S2 | `_listTiles.Find(t => t.name == tilemap).tile` — NullRef if tile name missing from `_listTiles` (still present in `RoomGeneraterController.cs:116,140`) | Sprint 2 |
| BUG-RC-1 | Map/Room | S2 | `RoomCell.GetDoor()` still does `new DoorController()` as a default — constructing a `MonoBehaviour` with `new` is invalid and risks a crash if a direction is ever missing | Sprint 2 |
| BUG-DDT-1 *(new)* | Map/Room | S3 | `RoomGeneraterController.DeleteDoorTileMap()` reads `this.Data` / `this.IndexLevelDataDoor`, both of which `ClearRoom()` empties at the end of its own run. If `DeleteDoorTileMap` (triggered by `ON_CLEAR_ENEMY`) can fire after `ClearRoom` for the same room, the loop becomes a silent no-op and door tiles are never removed. **Needs a Play Mode check on the room-clear → door-open sequence.** | Sprint 2 |

---

## P3/P4 Bugs — Backlog / Won't Fix

| ID | System | Severity | Summary | Disposition |
|----|--------|----------|---------|-------------|
| BUG-DC-2 | Map/Door | S4 | `DoorController.CheckCanBeOpened()` dead method — no body, no callers | Backlog |
| BUG-DC-3 | Map/Door | S4 | `string Name` field in `DoorController` shadows `UnityEngine.Object.name` | Backlog |
| BUG-RC-2 | Map/Room | S4 | `[SerializeField]` on auto-properties (`StartDoorPosition`, `ListDirectionDoors`, `IsCleared`, `Data`, `DoorPoints`, `IndexLevelDataDoor`) silently ignored by Unity — not visible in Inspector for debugging | Backlog |
| BUG-RC-3 | Map/Room | S4 | `[ExecuteInEditMode]` + `Instantiate` in `Setting()` — duplicates door prefabs in Editor | Backlog |
| BUG-CRC-1 *(new)* | Map/Room | S4 | `RoomGeneraterController.ClearRoom(ref RoomCell _current)` — `ref` on a reference type (`RoomCell` is a `class`) has no effect; cosmetic cleanup | Backlog |

---

## Systemic Issues Flagged

**🔴 Sprint-01 commitment not met — 0 of 6 Must-Have (P1) bugs fixed in the sprint window.**
All 6 P1 bugs (BUG-04, BUG-05, BUG-06, BUG-07, BUG-08, BUG-09) are byte-for-byte unchanged from the 2026-05-29 triage. This is the **4th consecutive weekly review** flagging these exact bugs as demo blockers (5/26, 5/29, 6/05, 6/12) — and the sprint-01 risk register explicitly named this pattern as the top risk ("3 straight weeks of effort went to Map refactors instead of these exact blockers"). The pattern repeated for a 4th week, this time *during* the sprint meant to fix it.

**Map/Room system remains the activity hot spot, contrary to the sprint-01 freeze.**
All 3 commits with `.cs` changes this week touched `Assets/Script/Map/**` (`MapGridController`, `MapCell`, `RoomCell`, `RoomGeneraterController`, `RoomGridController`). Sprint-01 explicitly called for freezing new Map/Room work for the sprint duration ("Freeze new Map/Room work for the sprint; fix only what blocks testing the Must Haves"). One new P2 bug (BUG-DDT-1) was introduced in this out-of-scope work.

**New third-party dependency added without an ADR.**
DOTween (`Assets/Plugins/Demigiant/DOTween/`) was added this week (commit `b2fb184`) and is now used in `MapGridController`. Per `.claude/rules` and architecture conventions, new dependencies should have an ADR recording the decision (Unity built-in tweening vs. DOTween, license, maintenance). Not a bug, but flag for `/architecture-decision`.

---

## Trend Analysis

**Volume**: 15 open (13 carryover + 2 new) / +2 net change this week
**Hot spot**: Map/Room system — 8 of 15 open bugs (was 10 of 16 on 5/29; 3 likely resolved, 1 new added)
**Regressions**: 0 bugs against previously-completed/marked-done stories
**Aged bugs (>2 sprints old)**: BUG-04, BUG-05, BUG-06, BUG-07, BUG-08, BUG-09 — all 6 P1 bugs are now effectively 3 weeks old and were the entire stated scope of sprint-01, which just closed without touching them.

> ⚠️ 6 S1/S2 bugs that were the explicit Must-Have scope of sprint-01 remain unassigned-and-unfixed at sprint close. This is the project's primary risk and must be addressed in sprint-02 kickoff before any new scope is accepted.

---

## Recommended Actions

1. **Sprint-02 kickoff must re-commit BUG-04, BUG-06, BUG-09, BUG-05/07/08 as the *only* Must-Haves**, with an explicit code freeze on `Assets/Script/Map/**` enforced (not just stated) — e.g., by not assigning any Map/Room tasks to sprint-02 at all. Run `/sprint-plan new` with this constraint.
2. **Verify BUG-RGC-1/2/3 are actually resolved** via `/smoke-check` before closing them — the room-loading rewrite this week likely fixed them but no Play Mode confirmation exists.
3. **Investigate BUG-DDT-1 (`DeleteDoorTileMap` data lifecycle)** in Play Mode — confirm doors are removed correctly on `ON_CLEAR_ENEMY` after this week's `IndexLevelDataDoor` refactor.
4. **Open `/architecture-decision` for the DOTween dependency** — record the choice before it spreads to more systems (currently only `MapGridController`).
