# Bug Triage Report

> **Date**: 2026-06-12
> **Mode**: sprint (Sprint 1, 2026-06-08 to 2026-06-12 — final day of sprint)
> **Generated by**: /bug-triage
> **Open bugs processed**: 15 (carried from 2026-05-29 triage, re-verified against current source)
> **Sprint in scope**: Sprint 1 — `production/sprints/sprint-01.md`

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix this sprint (now carries to Sprint 2) | 6 | All 5 Sprint 1 Must Have tasks unstarted; all block playable demo |
| P2 — Fix soon | 3 | Map/minimap quality issues, one new this week |
| P3 — Backlog | 4 | No gameplay impact |
| P4 — Won't fix | 0 | — |

**Critical (S1/S2) unfixed count**: 9
**Fixed since last triage (2026-05-29)**: 2 — BUG-RGC-1, BUG-RGC-3

---

## P1 Bugs — Fix This Sprint (carry to Sprint 2)

| ID | System | Severity | Summary | Story | Status this week |
|----|--------|----------|---------|-------|-------------------|
| BUG-04 | Combat | S1 | `WeaponMelee.Attack()` foreach body still empty — no `INegativeReceiver.TakeDamage()` call (`WeaponMelee.cs:29-33`) | S1-01 | UNCHANGED |
| BUG-09 | Animation | S1 (rated Critical by sprint plan — "all combat states permanently stuck") | `AnimationPlayerController.OnEnable()` registers `StartAnimation` twice; `EndAnimation` callback never fires (`AnimationPlayerController.cs:21`) | S1-02 | UNCHANGED |
| BUG-06 | Game Loop | S1 | `Core.TakeDamage()` has no death check, no `ON_PLAYER_DEATH` emit; `EventID` enum has no `ON_PLAYER_DEATH` entry (`Core.cs:21-25`) | S1-03 | UNCHANGED |
| BUG-05/07/08 | Enemy AI | S2 | Death chain cluster: `EntityMoveState.LogicUpdate()` still dereferences `entity.Input.Target.transform.position` (line ~29) before the null check on the next line; `EntityDeathState` still extends `MonoBehaviour` not `EntityState`; `EntityBasicState.LogicUpdate()` `Health <= 0` block still empty | S1-04 | UNCHANGED |
| (blocked) | Room Progression | S1 | Room-clear lock/unlock on enemy count — cannot start until S1-04 lands | S1-05 | UNCHANGED (blocked) |
| BUG-RC-1 | Map/Room | S1 | `RoomCell.GetDoor()` still constructs `new DoorController()` (a MonoBehaviour) via `new` instead of returning `null` for an unmatched direction (`RoomCell.cs:54-62`) | Demo blocker (Map) | UNCHANGED |

---

## P2 Bugs — Fix Soon

| ID | System | Severity | Summary | Target Sprint |
|----|--------|----------|---------|---------------|
| BUG-RGC-4 | Map/Room | S2 | `_listTiles.Find(t => t.name == tilemap).tile` — NullRef if tile name missing from `_listTiles` (`RoomGeneraterController.cs`) | Sprint 2 |
| BUG-MC-1 (new) | Map/Minimap | S3 | `MapCell.Setting()` calls `this.gameObject.SetActive(IsVisited)` with `[ExecuteInEditMode]` on the class — on first Setting() pass (`IsVisited = false` by default) this deactivates every minimap cell GameObject, including in Edit Mode, which can persist the disabled state into the saved scene/prefab. Same family of risk as BUG-RC-3 | Sprint 2 |
| BUG-DC-1 | Map/Door | S3 | `OpenDoor()` always sets `OPEN`, never `BE_OPEN` — receiving-side door has wrong status | Before status-gated mechanic |

---

## P3/P4 Bugs — Backlog / Won't Fix

| ID | System | Severity | Summary | Disposition |
|----|--------|----------|---------|-------------|
| BUG-RC-2 | Map/Room | S4 | `[SerializeField]` on auto-properties silently ignored — `StartDoorPosition`/`ListDirectionDoors` not in Inspector | Backlog |
| BUG-DC-2 | Map/Door | S4 | `DoorController.CheckCanBeOpened()` dead method — no body, no callers | Backlog |
| BUG-DC-3 | Map/Door | S4 | `string Name` field in `DoorController` shadows `UnityEngine.Object.name` | Backlog |
| BUG-RC-3 | Map/Room | S4 | `[ExecuteInEditMode]` + `Instantiate` in `Setting()` — duplicates door prefabs in Editor | Backlog |

---

## Fixed Since Last Triage (2026-05-29)

| ID | Summary | Verified by |
|----|---------|-------------|
| BUG-RGC-1 | `RoomGridController.OnDisable()` now correctly calls `UnResgister` for all three events (was `Resgister`) | Read of `RoomGridController.cs:33-38` |
| BUG-RGC-3 | `swapLevelData`, door-tile index list (`IndexLevelDataDoor`, replacing `CurentDoorLevelData`), and `DoorPoints` are now cleared in `ClearRoom()`/`Data.Clear()` — refactored this week in `b2fb184` | `RoomGeneraterController.cs` diff in `b2fb184` |

---

## Systemic Issues Flagged

### 1. Sprint 1 commitment missed entirely — 4th consecutive week of demo-blocker carryover
Sprint 1 (`production/sprints/sprint-01.md`, 2026-06-08 to 2026-06-12) committed **5 Must Have tasks** (S1-01 through S1-05) explicitly framed as "the minimum slice needed to playtest combat for the first time." As of today (the sprint's last day), **zero of these tasks show any code change**:
- S1-01 (`WeaponMelee.Attack`) — unchanged
- S1-02 (`AnimationPlayerController`) — unchanged
- S1-03 (`Core.TakeDamage` death check) — unchanged
- S1-04 (Enemy AI death chain) — unchanged
- S1-05 (room-clear lock/unlock) — blocked on S1-04, unchanged

Instead, all 5 commits this week (`220c7cf`, `b2fb184`, `1a21050`, `be6c4cd`, `527a00d`) went to Map/Minimap refactoring and DOTween integration. Sprint 1's own risk register explicitly flagged this exact pattern as the top risk: *"Pattern risk: 3 straight weeks of effort went to Map refactors instead of these exact blockers."* This is now the **4th** consecutive week.

**This is a process issue, not a code issue** — it should be raised at the next sprint planning session before re-committing the same 5 tasks again.

### 2. Map/Room system remains the hot spot, but improving
Map/Room bugs dropped from 10 (2026-05-29) to 7 open (BUG-RGC-4, BUG-RC-1, BUG-MC-1, BUG-DC-1, BUG-RC-2, BUG-DC-2, BUG-DC-3, BUG-RC-3 — minus 2 fixed). Real progress was made here this week, just not on the committed sprint scope.

### 3. Enemy AI death chain cluster (BUG-05/07/08)
Still 3 bugs in the same `EntityState` death chain, unchanged since 2026-05-29 — confirmed they should be fixed atomically in one PR per sprint-01.md S1-04.

---

## Trend Analysis

**Volume**: 13 open / -2 net change this triage (2 fixed, 0 new P1/P2... but +1 new P3 found: BUG-MC-1)
**Hot spot**: Map/Room system — 7 of 13 open bugs (down from 10/16)
**Regressions**: 0 bugs against previously-completed stories
**Aged bugs (>2 sprints old)**: BUG-04, BUG-05, BUG-06, BUG-07, BUG-08, BUG-09, BUG-RC-1 — 7 S1/S2 bugs, all unassigned to any completed sprint, all originated at project start (~3+ weeks unresolved)

> ⚠️ 7 S1/S2 bugs have been open for more than 2 sprints with no code progress, despite being the explicit subject of a dedicated sprint (Sprint 1) that just concluded with 0% completion on its Must Have items. This is the most severe finding of this triage.

---

## Recommended Actions

1. **Sprint retrospective is mandatory before Sprint 2 planning.** Run `/retrospective` to address why Sprint 1's Must Have items (all framed as 0.25-0.5 day fixes, totaling ~2 days of a 4-day available capacity) received zero attention while ~5 days of unplanned Map/Minimap/DOTween work landed instead. Without addressing this, re-committing the same 5 items to Sprint 2 will likely repeat the pattern.
2. **If Sprint 2 re-commits S1-01 through S1-05, time-box them to the first 2 days** and freeze all Map/Minimap work until they pass acceptance criteria — per sprint-01.md's own mitigation plan, which was not followed.
3. **Fix BUG-RC-1** (`RoomCell.GetDoor()` `new DoorController()`) alongside S1-04/S1-05 since it's also a Map/Room demo blocker and is a 5-minute fix (return `null`, caller already null-checks at line 98).
4. **Triage BUG-MC-1** (new, MapCell `SetActive` in `[ExecuteInEditMode] Setting()`) — verify in Editor whether minimap cells are getting stuck disabled in the saved scene; if so this could silently break the just-refactored minimap.
