# Bug Triage Report

> **Date**: 2026-09-06
> **Mode**: sprint (Saturday weekly wrap-up, `pm-weekly-wrapup`, autonomous)
> **Generated by**: PM assistant, code-review pass over `sprint-12` HEAD `f3f5f08`
> **Open bugs processed**: 6 (BUG-052, BUG-053, BUG-063, BUG-064, BUG-065, BUG-066)
> **Sprint in scope**: sprint-12 (closing) → carries into sprint-13
> **Baseline for comparison**: `production/qa/bug-triage-2026-08-30.md` (last triage, sprint-11 wrap-up)
> **Diff reviewed**: `6348dc6..f3f5f08` (last wrap-up commit → current HEAD), 87 changed `.cs` files —
> the largest single-week diff on record for this project (Entity/Stat refactor completion, new
> VContainer DI layer, new Item/loot system, folder restructure into `Assets/Script/System/`)

---

## Summary Counts

| Metric | Count | Delta vs. 2026-08-30 |
|---|---|---|
| Bug files on disk | 6 | +2 (BUG-065, BUG-066 filed this cycle) |
| Closed/fixed this cycle | 2 (BUG-053 fixed in source; BUG-064 partially fixed — 6 of 7 sub-items) | — |
| Still open, unchanged | BUG-063 (24+ cycles), BUG-052 (doc-sync gap, now larger in scope) | — |
| New this cycle | BUG-065 (`PlayerDeathState` movement-stop regression), BUG-066 (`EntityVitalStats` dictionary key risk) | +2 |

**Bottom line**: Sprint 12 made real, verifiable progress on its stated goal — the compile break
(BUG-064) is substantially fixed (6 of 7 sub-items) and BUG-053 (enemy health routing) is genuinely
fixed in source. But the sprint's single cheapest item (BUG-063, one line, a comment in the file
explaining exactly what to do) is still untouched after 24+ cycles, zero of the sprint's five
process/debt Should-Have items landed, and **no Play Mode / Editor session has occurred at any point
in Sprint 12** — every fix above is verified by source reading only, not a running build.

---

## Per-Bug Status

### BUG-064 — Project does not compile at HEAD
**Status: PARTIALLY FIXED.** Sub-items 1-6 (dangling `EntityStatsSO`/`Entity.Data`/`EntityFindTarget`
references, `ON_ENEMY_DEATH` payload cast mismatch, `EnemySpawner.OnDisable()` `Resgister`/`UnResgister`
typo) all verified fixed against current source. **Sub-item 7 still open**: `RangeWeapon.cs:7`'s
`IObjecPoolService poolManager` has no `[Inject]` wiring — ranged combat is silently dead. The fix
pattern already exists in the same codebase (`ItemSpawner.cs`) — this is a 2-3 line mirror, not new
design work. See `production/qa/bugs/BUG-064.md` for the full per-item table.
**Severity**: S1 (item 7 alone). **Priority**: P1 — first item for sprint-13, exactly as flagged at
every standup since Friday.

### BUG-063 — `Stat.modifiers` re-serialization regression
**Status: OPEN, unchanged.** `Stat.cs:63-65` still wraps `[SerializeField]` in `#if UNITY_EDITOR`
directly beneath a comment block explaining not to do exactly that. Now the single most persistently
carried item in the project's triage history (24+ consecutive cycles). No technical blocker has ever
been identified — this has lost every session to higher-priority work, cycle after cycle.
**Severity**: S1 (active data-corruption risk on checked-in `.asset` files, per ADR-0001).
**Priority**: P1 — recommend this be a literal, isolated first commit of sprint-13, decoupled from
any other task, given the demonstrated pattern of it losing priority every time it's bundled with
larger work.

### BUG-053 — `EntityNegativeReciver` player-only logic on an enemy
**Status: FIXED (source-verified).** Component fully rewritten — routes damage through
`EntityVitalStats`/`EntityStatsHandler`, no more `PlayerInputHandler`/`ON_PLAYER_DEATH` misuse.
Recommend closing this file formally once an in-Editor Play Mode pass confirms no NRE on enemy hit.
**Severity**: was S1, now resolved pending verification.

### BUG-052 — Undocumented live subsystems (doc-sync gap)
**Status: OPEN, scope has grown substantially.** The original finding (`Character/Base/`,
`Pathfinding/`, `Poolable/` missing from CLAUDE.md) is now compounded by this week's 298-file folder
restructure (`Assets/Script/{Enemy,Pathfinding,Skill_Ability,StatSystem,...}` moved under
`Assets/Script/System/`), a new VContainer DI layer (`System/LifetimeScope/`), and a brand-new
Item/loot system (`System/Item/`, 9+ files) — none reflected in CLAUDE.md's Repository Layout, which
still documents pre-restructure paths throughout. Any agent orienting from CLAUDE.md alone will read
stale file paths for roughly half the codebase.
**Severity**: S3 (documentation drift, not a runtime bug) — but now large enough to actively mislead.
**Priority**: escalate to P1 for sprint-13 given the scale of drift; `/doc-sync` should be treated as
blocking, not a Should-Have that keeps losing to code work (S12-10 did not land this cycle either).

### BUG-065 (NEW) — `PlayerDeathState` no longer stops `PlayerMovement`
**Status: OPEN.** CLAUDE.md credits BUG-044 with fixing this; current `PlayerDeathState.cs` shows the
movement-stop call is gone from `Enter()`. Either reverted in a merge or the doc claim was already
stale. See `production/qa/bugs/BUG-065.md`.
**Severity**: S2 (wrong behaviour, not a crash). **Priority**: P2 — bundle with the next Player-state
touch, or as a quick isolated fix given the file is small and the fix is a one-method addition.

### BUG-066 (NEW) — `EntityVitalStats` unguarded dictionary indexer
**Status: OPEN.** `ReceiveReduction`/`ReceiverRecovery`/`GetCurrentStatValue` index
`currentStats[statType]` directly; a `StatType` missing from an entity's authored profile throws
`KeyNotFoundException` on the live damage chain. See `production/qa/bugs/BUG-066.md`.
**Severity**: S2 (not yet observed to fire, but the exact class of risk `ai-code.md`'s Null Safety
section requires guarding against). **Priority**: P2 — natural first assertion for S12-11's planned
first EditMode test on the Entity damage chain.

---

## Systemic Trends

1. **Trivial, zero-blocker items are structurally the ones that never win a session.** BUG-063 (one
   line, 24+ cycles), the pre-push hook (19+ cycles), and the ADR-0002 sign-off (13+ cycles) all share
   this pattern: no technical blocker, explicitly flagged "land first" repeatedly, still untouched.
   This is now the single most consistent finding across the last four triage reports — larger,
   more urgent work keeps displacing them regardless of how the sprint plan sequences them.
2. **Documentation drift keeps outpacing `/doc-sync`.** CLAUDE.md's Repository Layout is now stale
   against roughly half the codebase (see BUG-052 update above). This is the third consecutive triage
   cycle to flag a doc/code disagreement of increasing scale.
3. **Zero Play Mode verification has occurred across Sprint 11 or Sprint 12.** Every "fixed" status in
   this report (BUG-053, BUG-064 items 1-6) is a source-read verification, not a confirmed running
   build. This is now a 6+ sprint pattern (per prior retros' S11-07 tracking) and is the reason no bug
   in this backlog can currently be marked definitively Closed rather than "fixed pending verification."
4. **When a large refactor did land this week (87 files), it substantially fixed two long-stuck S1
   bugs (BUG-064 mostly, BUG-053 fully) in one pass** — evidence that the entity/stat rework was the
   right shape of fix, just still missing its last DI-wiring step and any verification evidence.

---

## Recommended Actions

1. **Finish BUG-064 item 7** (`RangeWeapon.cs` DI wiring) — smallest remaining piece of the sprint's
   stated goal, pattern already proven same-repo.
2. **Get one owner-in-Editor session to confirm Console-clean + fire the ranged weapon once** — this
   is the single highest-leverage action available; nothing in this report is verifiable without it.
3. **Land BUG-063 as a fully isolated, single-purpose commit** — do not bundle with any other task,
   given the demonstrated 24-cycle pattern of it losing priority when bundled.
4. **Escalate `/doc-sync` to blocking for sprint-13** given the scale of drift documented under BUG-052.
