# Bug Triage Report

> **Date**: 2026-09-13
> **Mode**: sprint
> **Generated by**: `/bug-triage` (autonomous, `pm-weekly-wrapup` Saturday run)
> **Open bugs processed**: 9 (10 filed, 1 closed — BUG-053). **4 new this cycle**: BUG-067/068/069/070,
> surfaced by this run's `/code-review` pass over the week's Abilities v2 + Vitals-split diff.
> **Sprint in scope**: sprint-13 (`production/sprints/sprint-13.md`, 2026-09-08 → 2026-09-12, branch `sprint-13`)

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix this sprint | 6 | BUG-063, BUG-064 (item 7), BUG-052 (doc debt) carried; **BUG-067, BUG-068 new** (both S1, both reachable this week) |
| P2 — Fix soon | 4 | BUG-065, BUG-066 carried; **BUG-069, BUG-070 new** |
| P3 — Backlog | 0 | — |
| P4 — Won't fix | 0 | — |

**Critical (S1/S2) unfixed count**: 8 (BUG-063 S1, BUG-064 S1-partial, BUG-065 S2, BUG-066 S2, BUG-067 S1, BUG-068 S1, BUG-069 S2, BUG-070 S2)

---

## P1 Bugs — Fix This Sprint

| ID | System | Severity | Summary | Assigned to | Story |
|----|--------|----------|---------|-------------|-------|
| BUG-063 | StatSystem | S1 | `Stat.cs:63-66` still gates `[SerializeField]` behind `#if UNITY_EDITOR` on `modifiers` — re-verified unchanged at HEAD. One-line documented fix, carried since 2026-08-22 (≈4 sprints / 25th+ triage cycle mention) | sprint-13 (was S13-03, not landed) | — |
| BUG-064 | Weapons / DI | S1 (item 7 only) | `RangeWeapon.cs:7` still `[SerializeField] private IObjecPoolService poolManager` with no `[Inject] Construct(...)` — re-verified unchanged. Ranged weapon still silently non-functional. Items 1-6 remain fixed | sprint-13 (was S13-01, not landed) | — |
| BUG-052 | Cross-cutting docs/ADR | S3 (widened scope) | `Character/Base/`, `Pathfinding/`, `PoolableService/` still lack an ADR; **new since last triage**: TD-040 (no ADR deciding convergence of Abilities v1 vs v2) is now the single largest undocumented decision, and this week's "done prototype ability system" commit (`0622b07`) shipped more Abilities v2 code with still no ADR | sprint-13 (was S13-07 partially — `/doc-sync` did land, CLAUDE.md is current again, but the underlying ADR gaps it describes remain open) | — |
| **BUG-067** (new) | Player / Vitals / Item | S1 | `ResourceReceiver.ReceverRecovery`/`ReceiveReduction` call the wrong `VitalStatsComponent` method — **swapped**. A healing item pickup currently damages the player (`RecoveryEffectDefinition.Apply()` is a live, reachable caller) | Sprint 14 | — |
| **BUG-068** (new) | Abilities v2 / Player | S1 | `AbilityHolder.GetAbility()` dereferences `currentAbility.Definition` with no null check after a `TryGetValue` miss — NRE on the first skill press if any `AbilitySlot` is unbound. Directly threatens S13-02's planned clean-console Play Mode smoke session | Sprint 14 | — |

---

## P2 Bugs — Fix Soon

| ID | System | Severity | Summary | Target Sprint |
|----|--------|----------|---------|---------------|
| BUG-065 | Player / State Machine | S2 | `PlayerDeathState.Enter()` still only calls `base.Enter()` — re-verified unchanged at HEAD. Player still slides during death | Sprint 14 (was S13-04, not landed) |
| BUG-066 | Enemy AI / StatSystem | S2 | `EntityVitalStats.cs` — `GetCurrentStatValue`/`ReceiverRecovery`/`ReceiveReduction` still index `currentStats[statType]` raw, no `TryGetValue` guard — re-verified unchanged at HEAD | Sprint 14 (was S13-05, not landed) |
| **BUG-069** (new) | Abilities v2 / Player | S2 | `AbilityInstance.CanStart()` (the cooldown gate) has zero callers project-wide — every player ability can be re-triggered with no cooldown wait | Sprint 14 | — |
| **BUG-070** (new) | Player / StatSystem | S2 | `VitalStatsComponent` repeats BUG-066's exact unguarded-dictionary pattern on the player side (`GetCurrentStatValue`/`ReceiverRecovery`/`ReceiveReduction`) — recommend fixing both in one pass | Sprint 14 | — |

---

## P3/P4 Bugs — Backlog / Won't Fix

None this cycle.

---

## Systemic Issues Flagged

1. **Sprint-13's entire Must-Have list failed to land, for the 3rd+ consecutive sprint in a row.**
   Verified directly against source, not commit messages: S13-01 (BUG-064 item 7), S13-03 (BUG-063),
   S13-04 (BUG-065), and S13-05 (BUG-066) are **all** byte-for-byte unchanged from the state
   sprint-13 opened with. These were explicitly scoped as the cheapest, most isolated fixes in the
   backlog (combined estimate 0.4d) specifically *because* they had lost to larger work every prior
   cycle — the same failure mode repeated a third time despite the mitigation named in
   `sprint-13.md`'s own Risks table ("Daily plan schedules the cheapest items as a dedicated block at
   the very start of Day 1").

2. **Off-plan work shipped instead.** The week's actual commits (`9b8d40f`, `5c7afba`, `4e4eff5`,
   `9f1258c`, `e1c9606`, `a4d1793`, `0622b07` — "coding", "prototy ability effect", "done prototype
   ability system") completed the Abilities v2 promotion (`prototypes/skill-enhance-abilities/` →
   `Assets/Script/System/Abilities/`) and the Item/ResourceReceiver system. None of this traces to a
   sprint-13 task ID. This is the same "off-plan work" pattern BUG-052 and six prior triage cycles
   have already flagged — the work is real and appears substantial, but it was never scoped, so its
   quality/risk was never gated by a story or QA plan.

3. **Should-Have items landed, Must-Have items did not — inverted priority.** S13-07 (`/doc-sync`,
   0.4d) and S13-10 (ADR-0004, 0.3d, via the same doc-sync commit) both completed. The 0.4d task
   landed while the four 0.05–0.15d tasks did not, meaning effort this week tracked the unplanned
   Abilities work, not the sprint plan's stated priority order.

4. **QA plan gap is now 26th+ consecutive cycle** — no `production/qa/qa-plan-sprint-13.md` exists.

5. **The unplanned Abilities v2 work that displaced the sprint plan (issue #2 above) shipped with
   its own new S1/S2 bugs** (BUG-067, BUG-068, BUG-069, BUG-070) — reinforcing that off-plan work
   is landing without the QA plan / review gate that scoped work goes through. BUG-067 in particular
   (heal/damage swap) is a functional regression on a path a real player would hit immediately.

---

## Trend Analysis

**Volume**: 9 open / **+4 net change this sprint** (BUG-067/068/069/070 opened, none closed)
**Hot spot**: Abilities v2 / player vitals (`Assets/Script/System/Abilities/`,
`Assets/Script/Character/Player/CoreComponent/VitalComponent.cs` + `ResourceReceiver.cs`) — **4 of
the 4 new bugs this cycle** come from the one subsystem that shipped unplanned this week. This is
the sharpest hot-spot signal in recent triage history: a brand-new system with zero test coverage
generated 4 S1/S2 bugs in its first reviewed week.
**Regressions**: 0 bugs filed against previously-completed *stories* (no story exists for the
Abilities v2 work, so BUG-067 cannot technically be a "regression" in the tracked sense — but it is
a regression against the working healing behaviour that existed before this week's `ResourceReceiver`
changes)
**Aged bugs (>2 sprints old)**: 2 — BUG-063 (opened ~2026-08-22, now spanning sprint-11 → sprint-13)
and BUG-052 (opened 2026-08, widened twice since)

> ⚠️ BUG-063 (S1) has been open, fully diagnosed, with a one-line fix, for roughly 4 sprints and
> 25+ documented triage/standup mentions. This is now the single most persistently-carried item in
> the project and represents accepted risk that should be explicitly reviewed by the owner rather
> than re-carried a 5th sprint by default.

---

## Recommended Actions

1. **Fix BUG-067 (heal/damage swap) first — it is a live, reachable player-facing regression**, then
   BUG-068 (NRE on unbound ability slot), since both threaten the still-unrun S13-02 Play Mode smoke
   session directly. Both are small, isolated fixes per their bug files.
2. **Land BUG-063 and BUG-064 item 7 as standalone commits before any other sprint-14 work** — both
   fixes are fully specified in their bug files and have been for multiple cycles; no further
   diagnosis is needed, only the commit.
3. **Decide explicitly whether Abilities v2 completion should have displaced sprint-13's stated
   Must-Have goal.** If yes, retroactively re-scope sprint-13 as a FAIL with the Abilities work
   recorded as the real (unplanned) deliverable; if no, sprint-14 planning should protect the
   cheap-fix block with a harder gate (e.g., blocking commit order) rather than a schedule note, since
   the schedule-note mitigation has now failed 3 times. Either way, the 4 new bugs this cycle show the
   off-plan work needs the same review gate as scoped work, not less.
4. File an ADR (or explicit backlog decision) resolving TD-040 (Abilities v1 vs v2 convergence) before
   a fourth sprint of v2 work ships with the decision still open.
5. Fix BUG-066 and BUG-070 together (identical unguarded-dictionary shape on the enemy and player
   vitals components respectively) and consider a shared guarded-lookup helper to prevent a third
   recurrence.
