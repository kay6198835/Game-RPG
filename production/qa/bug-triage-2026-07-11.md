# Bug Triage Report

> **Date**: 2026-07-11
> **Mode**: sprint (Sprint 5 — `sprint-05` branch; sprint execution window 2026-07-14→2026-07-18 has
> not started yet, 0 days elapsed)
> **Generated by**: `/weekly-wrapup` (automated Saturday 22:00 run)
> **Branch reviewed**: `sprint-05` @ `89cad88` (diff since last triage `1d9fab3`/`bug-triage-2026-07-10.md`)
> **CS files reviewed**: 1 file with real changes — `Assets/Script/Enemy/EnemySpawner.cs` (uncommitted,
> staged: 3 new `[SerializeField]` fields + padding logic in `SpawnRoomEnemies()`, matches the carried
> WIP `cb099ee`/stash referenced by Sprint 5 task S5-D1). No other `.cs` files changed since last
> triage — the week between triages was docs-only (GDD/epic/ADR re-sync, sprint renewal) plus this one
> scene + code WIP commit/stage.
> **Note**: Carried bug IDs re-verified directly against current code, not assumed from last report.

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix this sprint | 4 | 0 resolved since last triage; 1 new (unguarded `spawnPosition` NRE/IndexOutOfRange in the padding code path) |
| P2 — Fix soon | 12 | 11 unchanged; 1 new (unclamped padding can spawn enemies inside walls/outside room) |
| P3 — Backlog | 7 | 6 unchanged; 1 new (padding scratch values needlessly `[SerializeField]`) |
| P4 — Won't fix | 0 | — |

**Critical (S1/S2) unfixed count**: 4
**Fixed since last triage (2026-07-10)**: 0
**New bugs found this triage**: 3 (P1: 1 — BUG-ES-4; P2: 1 — BUG-ES-6; P3: 1 — BUG-ES-5)

---

## P1 Bugs — Fix This Sprint

| ID | System | Severity | Summary | Assigned to |
|----|--------|----------|---------|-------------|
| BUG-06 | Player Damage Chain | S1 | `NegativeReciver.TakeDamage()` still `throw new NotImplementedException()` — player cannot take damage or die (`Assets/Script/Character/Player/CoreComponent/NegativeReciver.cs:8-11`). Unchanged since last triage. | Sprint 5 (S5-B2) |
| BUG-05/07/08 | Enemy AI Death Chain | S1 | Cluster unchanged: `EntityMoveState` null-deref before guard, `EntityDeathState` extends `MonoBehaviour` not `EntityState`, `EntityBasicState` death block still empty | Sprint 5 (S5-B3/B4/B5) |
| BUG-ES-1 | Enemy Spawn | S1 | `RoomModel.GetSpawnSet()` returns `null` when `enemiesOfRoom.Count == 0`; `EnemySpawner.GetRoomSpawnSet()` only null-checks `roomModel`, not the return value; `SpawnRoomEnemies()` calls `set.Count` on a possibly-null list → NullReferenceException. Unchanged since last triage — still live in the exact code path the new padding WIP sits next to. | Sprint 5 (S5-C1) |
| BUG-ES-4 (NEW) | Enemy Spawn | S1 | `EnemySpawner.SpawnRoomEnemies()` reads `spawnPosition[Random.Range(0, spawnPosition.Count)]` (`EnemySpawner.cs:60`) with no null/empty guard on `spawnPosition` itself. `spawnPosition` is only assigned in `OnDoneLoadRoomGrid()` — if `SpawnRoomEnemies()` is invoked any other way (e.g. the parallel `LevelManager.SpawnRoomEnemies()` editor-button driver, BUG-ES-2) before that callback fires, `spawnPosition` is `null` → NullReferenceException on `.Count`; if it fires but the marker list is empty, `Random.Range(0, 0)` returns `0` → `IndexOutOfRangeException` on the indexer. The new padding lines (`EnemySpawner.cs:61-64`) sit directly downstream of this unguarded read, so the carried WIP inherits the crash risk without adding a guard. | Enemy-Spawn epic, same fix window as BUG-ES-1 |

---

## Resolved Since Last Triage

None. No `.cs` fixes landed between 2026-07-10 and 2026-07-11 — the week's work was docs re-sync
(GDD/epic/ADR) plus the uncommitted `EnemySpawner.cs` padding WIP, which does not touch any bug on
the open list.

---

## P2 Bugs — Active

| ID | System | File | Summary | Sprint |
|----|--------|------|---------|--------|
| BUG-CORE-2 | Core | `Core.cs:7` | `[SerializeField]` on auto-property — silently ignored by Unity, field stays unassignable from Inspector | S5 backlog |
| BUG-RC-2 | Map/Room | `RoomCell.cs:13,14,18-21` | Same `[SerializeField]`-on-auto-property issue, 6 occurrences | S5 backlog |
| BUG-WM-2 | Combat | `WeaponMelee.cs:76,85` | `lastClickTime = Time.time` assigned twice in `SetAnimation()` — redundant | S5-D4 |
| BUG-WM-3 | Combat | `WeaponMelee.cs:87-101` | `DurationNextAttack()` returns `0` when no override clips — zero-cooldown attack spam | S5 backlog |
| BUG-AH-2 | AbilityHolder | `AbilityHolder.cs:5` | Leftover unused `using UnityEngine.UIElements;` | S5-D4 |
| BUG-EM-1 | Enemy Spawn | `Assets/Script/Enemy/EnemyManager.cs` | Still a bare scaffold — only `public static Instance` declared, no lifecycle. Blocks room-clear condition | Sprint 6 (per sprint-05.md scope) |
| BUG-EM-2 | Enemy Spawn | `EnemyManager.cs:2` | `using System.Numerics;` — unused, pre-empt future `Vector3` ambiguity | S5-D4 |
| BUG-15 | Map/Build | `RoomGeneraterController.cs`, `LevelManager.cs` | `File.ReadAllText(Application.dataPath + ...)` — Editor-only, won't ship in a Player build | Backlog |
| BUG-ES-2 | Enemy Spawn | `LevelManager.cs` | `LevelManager.SpawnRoomEnemies()` duplicates `EnemySpawner.SpawnRoomEnemies()` — two parallel spawn drivers, neither routes through `EnemyManager` (contradicts ADR-0002) | Sprint 5 (S5-C3) |
| BUG-ES-3 | Enemy Spawn | `EventManager.cs` `EventID` enum | Missing `ON_ENEMY_DEATH` / `ON_ROOM_CLEAR` / `ON_PLAYER_DEATH` | Sprint 5 (S5-B1) |
| BUG-LM-1 | Map/LevelManager | `LevelManager.cs:273` | `index = index == null ? 0 : index;` — non-nullable `int` compared to `null`, dead logic | S5-D4 |
| BUG-ES-6 (NEW) | Enemy Spawn | `EnemySpawner.cs:60-65` | Padding offset (`Random.Range(-maxPadding, maxPadding)`, default `maxPadding = 5f`) is applied to the picked spawn position with **no bounds check against room geometry** — since `spawnPosition` entries come from authored `Tile_Spawn_Enemy` marker positions (not padded/clamped room bounds), a large enough padding roll can push an enemy `Instantiate` point through a wall tile or outside the room. Not a crash, but a placement-quality bug; not covered by `design/gdd/enemy-spawn-system.md` Edge Cases section either. | Should be resolved as part of S5-D1 (WIP reapply) before it's called done, or flagged as a known limitation in the GDD |

---

## P3 Bugs — Backlog

| ID | System | Summary | Disposition |
|----|--------|---------|-------------|
| BUG-DC-2 | Map/Door | `DoorController.CheckCanBeOpened()` dead method, no callers | Backlog |
| BUG-DC-3 | Map/Door | `DoorController.string Name` shadows `Object.name` | Backlog |
| BUG-RC-3 | Map/Room | `[ExecuteInEditMode]` + `Instantiate` in `Setting()` | Backlog |
| BUG-17 | Map/Door | Dead no-op code: `DoorController.OpenDoor()`/`CheckCanBeOpened()`, `RoomCell.UpdateStatusDoor()` | Backlog |
| BUG-16 | Map/Room | `RoomType` enum not read at runtime — start/end room forced by list position | Backlog |
| BUG-12 | Architecture | `LevelManager` uses `public static Instance` singleton — violates "no new singletons" rule | Backlog — needs refactor to Inspector ref |
| BUG-ES-5 (NEW) | Enemy Spawn | `EnemySpawner.cs:10,12` — `paddingPosition` and `positionRandom` are declared as `[SerializeField]` instance fields but used purely as loop-scratch locals inside `SpawnRoomEnemies()` (reassigned every iteration, read once immediately after). No other code reads their persisted value. Should be local variables (`var`) — the current form just adds Inspector noise that changes on every spawn call and can get committed with whatever value happened to be last written on the SO/prefab instance. | Backlog — roll into S5-D1 cleanup or a follow-up pass |

---

## Systemic Issues Flagged

- **Carried WIP is still uncommitted and touches an already-known-unsafe code path.** The padding fields/logic in `EnemySpawner.cs` (Sprint 5 task S5-D1: "reapply `cb099ee` + stash") are staged but not committed. They land directly adjacent to BUG-ES-1's unguarded `set.Count` read and inherit a second, previously-unflagged crash risk (BUG-ES-4) on the unguarded `spawnPosition` read one line below. S5-D1's acceptance criteria ("reflects the carried padding fix **without reintroducing BUG-ES-1**") should be read as also covering BUG-ES-4 — both null/empty guards belong in the same pass as S5-C1.
- **No `.cs` fixes landed this week** — the gap between the last two triages (07-10 → 07-11) was entirely docs re-sync (GDD/epic/ADR renewal for Option C) plus the one WIP stage. Expected: Sprint 5's execution window (07-14→07-18) hasn't started, so this is a pre-sprint snapshot, not a sprint-in-progress one.
- **QA Plan still missing** — `production/qa/qa-plan-sprint-05.md` does not exist. `sprint-05.md` already flags this as a blocking gate before Track B (death chain) lands; still true, unchanged this triage.

---

## Trend Analysis

**Volume**: 23 open bugs (P1: 4, P2: 12, P3: 7) — net +3 vs last triage (20 → 23: 0 resolved, 3 new, all in the Enemy Spawn System's padding WIP)
**Hot spot this week**: Enemy Spawn System again — 8 of 23 active bugs now trace to it (BUG-ES-1 through BUG-ES-6, BUG-EM-1, BUG-EM-2), all still open
**Regressions**: 0 against previously completed stories
**Aged bugs (>3 sprints)**: BUG-06, BUG-05/07/08 — still open since the original bug sweep, now carrying into Sprint 5's dedicated Track B
**Note**: no P1 closures this triage (0 vs. 4 last week) — expected given 0 days of sprint-05 execution elapsed; not a regression in velocity, just a pre-sprint measurement window

---

## Recommended Actions

1. **When S5-C1 (BUG-ES-1 fix) lands, fix BUG-ES-4 in the same pass** — both are unguarded reads two lines apart in `SpawnRoomEnemies()`; splitting them into separate tasks risks shipping one guard without the other.
2. **Decide before committing S5-D1**: either clamp the padding to room bounds (resolve BUG-ES-6) or explicitly document it as a known limitation in `design/gdd/enemy-spawn-system.md` Edge Cases — right now it's neither.
3. **When touching `EnemySpawner.cs` for S5-C1/S5-D1, convert `paddingPosition`/`positionRandom` to local variables** (BUG-ES-5) — zero-risk, same-file, same-sitting cleanup.
4. Carried from last triage, still applicable: fix BUG-06 (player death), the BUG-05/07/08 enemy-death cluster, resolve BUG-ES-2 (duplicate spawn driver), add the 3 missing `EventID` values (BUG-ES-3) — all already scoped into Sprint 5 Track B/C.
5. Run `/qa-plan sprint` before Track B implementation starts (Sprint 5 blocking gate, still open).
