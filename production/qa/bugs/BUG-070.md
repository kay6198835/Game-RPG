# BUG-070

**Title**: `VitalStatsComponent` indexes `currentStats[statType]` with no key-existence guard — same unguarded-dictionary pattern as `BUG-066`, now on the player side
**Severity**: S2 (would throw on the live player damage/heal chain for any `StatType` missing from the profile — not yet observed to fire)
**System**: Player / StatSystem
**Found by**: `pm-weekly-wrapup` code-review pass, 2026-09-13 (autonomous, `lead-programmer` agent via `/code-review`)
**Status**: Open — re-verified 2026-09-25 (HEAD `c0067f4`). **MERGED with BUG-066**: `VitalComponent.cs` is now a two-line shim and the unguarded dictionary lives in the shared `VitalStatsBase.cs:28,39,41,45,52,54,58`. One fix closes both. `:28` is still on the live death path. *Previous status line:* Open — CONFIRMED 2026-09-22. Same defect as BUG-066 (enemy side); fix and close together.

## Description

`Assets/Script/Character/Player/CoreComponent/VitalComponent.cs` — `GetCurrentStatValue` (line 26),
`ReceiverRecovery` (line 38), and `ReceiveReduction` (line 48) on `VitalStatsComponent` all index a
`Dictionary<StatType, float>` (`currentStats`) directly with no `TryGetValue`/`ContainsKey` guard,
seeded once from `StatHandler.GetFullStat()`. This is the exact same anti-pattern already open as
`[[BUG-066]]` on `EntityVitalStats` (the enemy-side twin of this component) — it has now propagated
to the player side that `ResourceReceiver`/`NegativeReciver` call into on every hit or pickup.

## Impact

- Any `StatType` not present in the seeded `currentStats` dictionary (e.g. a misconfigured
  `BaseStatsSO` profile, or a caller passing a `StatType` the player profile doesn't track) throws
  an unlabelled `KeyNotFoundException` on the player's damage/heal path — the mirror image of
  BUG-066's enemy-side risk.
- No EditMode test exists for either `VitalStatsComponent` or `EntityVitalStats` (TD-014) that would
  catch this before it reaches a running character.

## Suggested Fix

Same fix as BUG-066, applied here: use `TryGetValue` in all three methods, log + no-op or throw a
clear, named exception instead of the raw `KeyNotFoundException`. Recommend fixing BUG-066 and
BUG-070 together in one pass since the code shape is identical — a shared private helper
(`TryGetCurrentStat(StatType, out float)`) on a common base would prevent a third recurrence on
whichever vitals component is added next.

---

## Re-verification — 2026-09-22 (branch `main`, HEAD `d17fcc5`, merge of `e2cb75e`)

**Both still open. Recorded here for the first time: BUG-066 and BUG-070 are one defect with two
instances, not two independent bugs.** They should be fixed, tested and closed together — sprint-14
S14-06 and sprint-15 S15-04 already bundle them, and this confirms the bundling is correct.

### Both sites, at HEAD

Enemy — `Assets/Script/Character/Entity/CoreComponent/EntityVitalStats.cs`

| Line | Code |
|---|---|
| 39 | `return currentStats[statType];` |
| 49 | `if (currentStats[statType] + amount >= statHandler.GetStatValue(statType))` |
| 55 | `currentStats[statType] += amount;` |
| 61 | `if (currentStats[statType] - amount <= 0)` |
| 67 | `currentStats[statType] -= amount;` |

Player — `Assets/Script/Character/Player/CoreComponent/VitalComponent.cs` (class
`VitalStatsComponent`)

| Line | Code |
|---|---|
| 28 | `return currentStats[statType];` |
| 39 | `if (currentStats[statType] + amount >= statHandler.GetStatValue(statType))` |
| 41 / 46 | `currentStats[statType] = ...` / `+= amount` |
| 52 | `if (currentStats[statType] - amount <= 0)` |
| 54 / 58 | `currentStats[statType] = 0` / `-= amount` |

Neither file contains a single `TryGetValue` or `ContainsKey` call. The two classes are near-verbatim
copies of one another, which is why the defect exists twice.

### The player-side indexer is on the live death path

`PlayerBasicState.cs:74`

```csharp
if (vitalStats.GetCurrentStatValue(StatType.HP) <= 0)
```

runs every frame the take-damage flag is set, and goes straight to `VitalComponent.cs:28`.

### Two concrete ways this throws

**1. Initialisation-order race.** `currentStats` is populated only in `Start()`
(`VitalComponent.cs:19-24`). Unity does not guarantee `Start()` ordering between components, so any
component whose `Awake()`/`Start()` reads a vital value can run first and hit an empty dictionary.
The enemy side has a sharper version of this: `EntityVitalStats.OnEnable()` (`:26-29`) calls
`Reborn()`, and `OnEnable` runs **before** `Start()` — so on the first enable, `Reborn()`'s
`Core.GetCoreComponent(out statHandler)` (`:33`) may not resolve, and `statHandler.GetFullStat()`
(`:34`) throws or returns nothing.

**2. A `StatType` absent from the profile asset.** `BaseStatsSO.FullStatsValue()` (`:91-99`) adds
only the `StatType`s present in that SO's own stat list. Any `StatType` a designer did not author
into the asset is simply absent from the dictionary. That is not hypothetical: both
`HasEnoughManaCondition.IsMet()` (`HasEnoughManaCondition.cs:11`) and
`AbilityEffectDefinition.CheckPayCostValid()` (`:29`) look up
`Services.Vital.GetCurrentStatValue(cost.statType)` for a `StatType` chosen freely by whoever
authored the ability asset. Authoring an ability with a cost in a stat the character's profile does
not declare throws `KeyNotFoundException` at cast time.

### Fix (both files, one pass)

Replace every raw indexer with a guarded lookup. A shared helper is worth it precisely because the
duplication is what produced the second instance:

```csharp
private bool TryGet(StatType statType, out float value) => currentStats.TryGetValue(statType, out value);
```

and decide the miss policy once — return `0f`, or seed the key from the stat handler on demand. A
`KeyNotFoundException` from a dictionary the caller cannot see is the worst of the options.

Pair with the first EditMode test (TD-014) as sprint-14 S14-13 planned — but note that story is
blocked by **BUG-084** (no `.asmdef` anywhere, and `tests/` sits outside `Assets/`), so the test
cannot actually be written until that is resolved.

---

## Re-verification — 2026-09-25 (HEAD `c0067f4`) — STILL OPEN, and now the same file as BUG-066

The ADR-0005 refactor emptied `VitalComponent.cs` down to a two-line shim:

```csharp
// File name kept as VitalComponent.cs: PlayerTest.prefab references this script by GUID.
public class VitalStatsComponent : VitalStatsBase<Core>
{
}
```

Everything this bug describes moved to `Assets/Script/Character/Base/VitalStatsBase.cs`, which the
enemy side also derives from (`EntityVitalStats : VitalStatsBase<EntityCore>`). **BUG-070 and
BUG-066 are now literally the same seven lines**, not two parallel instances of one defect.

Unguarded `currentStats[statType]` at `VitalStatsBase.cs:28,39,41,45,52,54,58`. `:28` is still the
one on the live death path: `PlayerBasicState` reads `GetCurrentStatValue(StatType.HP)` every frame
to decide the transition into `PlayerDeathState`.

**New in this HEAD, and relevant to the fix:** the player now inherits `Reborn()`
(`VitalStatsBase.cs:20-24`), which re-seeds `currentStats` from `statHandler.GetFullStat()`. That is
the reset path BUG-087 was missing — but it is called only from `Start()` on the player side. The
enemy side additionally calls it from `OnEnable()` (`EntityVitalStats.cs:7-10`) because pooled
enemies are re-enabled rather than re-instantiated.

**Status**: unchanged — Open. **Merged with BUG-066**: one guard in `VitalStatsBase` closes both.
Suggested shape, to keep the death path fail-safe rather than fail-loud:

```csharp
public float GetCurrentStatValue(StatType statType)
    => currentStats.TryGetValue(statType, out float v) ? v : 0f;
```

Note that returning `0f` for HP would trigger the death transition, so seeding must be audited at
the same time — this is a two-part fix, not a one-liner.

Not verified in Play Mode — the project does not compile at this HEAD (**BUG-092**).
