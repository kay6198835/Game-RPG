# BUG-080

**Title**: `ResourceReceiver.vitalStatsComponent` is only ever assigned inside `ReceverModifierGroup()` — every other public method dereferences a null field
**Severity**: S1 (NullReferenceException on the live item-pickup path)
**Priority**: 1
**System**: Player / Core components / Item system
**Found by**: Documentation/source re-verification pass, 2026-09-22 (branch `main`, HEAD `d17fcc5`, merge of `e2cb75e`)
**Status**: ✅ FIXED 2026-09-25 (ADR-0005 refactor, merged in `379c267`) — `ResourceReceiver` is now `: Interact` with an empty body. The `vitalStatsComponent` field, `Recovery()`, `Reduction()` and `BuffDebuffForDuration()` are all gone, so there is nothing left to dereference unresolved. *Previous status line:* Open — CONFIRMED (static read; not run in Play Mode)

## Description

`Assets/Script/Character/Player/CoreComponent/ResourceReceiver.cs`:

```csharp
5:    VitalStatsComponent vitalStatsComponent;          // never initialised in Awake/Start
...
11:    public void ReceverModifierGroup(StatModifierGroup statModifierGroup)
13:        Core.GetCoreComponent<VitalStatsComponent>(out vitalStatsComponent);   // the ONLY writer
14:        vitalStatsComponent.ApplyBuffDebuff(statModifierGroup);
```

Line 13 is the only assignment to the field in the whole class. Three public methods read it
without ever resolving it:

| Line | Method | Reads |
|---|---|---|
| 19 | `Recovery(StatType, float)` | `vitalStatsComponent.Recovery(...)` |
| 23 | `Reduction(StatType, float)` | `vitalStatsComponent.Reduction(...)` |
| 27 | `BuffDebuffForDuration(...)` | `vitalStatsComponent.BuffDebuffForDuration(...)` |

`TakeDamage()` (`:29-35`) is unaffected: it resolves into a **local** variable that shadows the
field (`:31 Core.GetCoreComponent(out VitalStatsComponent vitalStatsComponent);`), so it never
touches the field at all. That shadowing is also why the bug has stayed invisible in damage
playtests.

This is reachable today. `RecoveryEffectDefinition.cs:13` calls
`IResourceReceiver.Recovery(StatType.HP, amount)` directly — picking up any recovery item before
any buff/debuff item has been picked up in the same session throws.

## Reproduction

1. Enter Play Mode in `LoadRandomMap.unity` with a fresh player.
2. Pick up an item whose `ItemSO.ItemEffects` contains a `RecoveryEffectDefinition`
   (do **not** pick up a `StatModifierEffectDefinition` item first).
3. Observe `NullReferenceException` from `ResourceReceiver.Recovery`.

## Suggested Fix

Resolve once in `Awake()`/`Start()` like every other core component does, and drop the per-call
resolution in `ReceverModifierGroup()` and the shadowing local in `TakeDamage()`:

```csharp
protected override void Start()
{
    base.Start();
    Core.GetCoreComponent(out vitalStatsComponent);
}
```

## Relationship to other bugs

- Split out of **BUG-067** during the 2026-09-22 re-verification pass. BUG-067's original defect
  (swapped heal/damage polarity) is fixed; this is a *different* defect that was living in the
  same file and was never separately filed.
- See also **BUG-081** (the duplicate `INegativeReceiver` implementation in the same file).

---

## Re-verification — 2026-09-25 (HEAD `c0067f4`) — FIXED by the ADR-0005 refactor

`ResourceReceiver` no longer holds the field this bug was about. The whole class is now:

```csharp
/// <summary>
/// Player pickup interactor. Items apply themselves to the owning ICharacter (see ItemController);
/// this component only finds and triggers the interaction. Kept as a class because
/// PlayerTest.prefab references it by GUID.
/// </summary>
public class ResourceReceiver : Interact
{
}
```

There is no `vitalStatsComponent` field, no `Recovery()`, no `Reduction()` and no
`BuffDebuffForDuration()` — so there is no half-resolved reference left to dereference, and the
NullReferenceException on the live item-pickup path is gone.

**Where the work moved**: items apply themselves to the owning `ICharacter` through
`ItemController`, and stat changes reach the character's `IVitalComponent` the way every other
system does under ADR-0005 Amendment 1 — `GetComponentInParent<ICharacter>()` then
`character.Transform.GetComponentInChildren<T>()`. `ResourceReceiver` kept its file name and class
name only because `PlayerTest.prefab` references the script by GUID.

**Status**: Open → **FIXED** (ADR-0005 refactor, merged in `379c267`).

Not verified in Play Mode — the project does not compile at this HEAD (**BUG-092**). The defect is
gone by construction: the field no longer exists.
