# BUG-067

**Title**: `ResourceReceiver` heal and damage calls are swapped — picking up a healing item damages the player
**Severity**: S1 (shipped, reachable functional regression on the new Item pickup path)
**System**: Player / Vitals / Item
**Found by**: `pm-weekly-wrapup` code-review pass, 2026-09-13 (autonomous, `lead-programmer` agent via `/code-review`)
**Status**: FIXED — verified 2026-09-22 (see re-verification below). Two unrelated defects found in the same file were split out as BUG-080 and BUG-081.

## Description

`Assets/Script/Character/Player/CoreComponent/ResourceReceiver.cs:17-24` has its two methods calling
the wrong `VitalStatsComponent` method:

```csharp
public void ReceverRecovery(StatType statType, float amount)
{
    vitalStatsComponent.ReceiveReduction(statType, amount);   // heal calls DAMAGE
}

public void ReceiveReduction(StatType statType, float amount)
{
    vitalStatsComponent.ReceiverRecovery(statType, amount);   // damage calls HEAL
}
```

This is not a theoretical path — `Assets/Script/System/Item/ItemEffectDefinition/RecoveryEffectDefinition.cs:13`
calls `resourceReceiver.ReceverRecovery(StatType.HP, amount)` when a player picks up a healing item.
Because the method bodies are swapped, that call reduces HP instead of restoring it. The new Item
pickup system (`853fe39`, `9f1258c`) and the `PlayerResourceReceiverState` added this sprint both
route through this exact entry point, so any healing consumable currently damages the player on pickup.

## Impact

- Direct, player-visible regression on the golden path of the new Item system: picking up a heal
  item hurts the player instead of healing them.
- No EditMode/PlayMode test exists to catch a swap like this (`tests/` — `.gitkeep` only, TD-014).

## Suggested Fix

Swap the two method bodies back: `ReceverRecovery` → `vitalStatsComponent.ReceiverRecovery(...)`,
`ReceiveReduction` → `vitalStatsComponent.ReceiveReduction(...)`. One-line-per-method fix; recommend
pairing with a first EditMode test asserting `ReceverRecovery` increases current HP and
`ReceiveReduction` decreases it, so this class of swap-bug can't regress silently.

## Resolution Note (2026-09-17 standup, verified against source)

The swap is **gone in current `sprint-14` source**, but not via the planned S14-01 fix. Commit
`1f61753` ("Update logic SO effect stats impact", Wed 2026-09-16, landed off-plan on
`origin/feature/fix-player-control`, merged into `sprint-14` via `4f31cb6` early Thu 2026-09-17)
renamed the methods (`ReceverRecovery`/`ReceiveReduction` → `Recovery`/`Reduction` on both
`IResourceReceiver` and `IVitalComponent`) while doing unrelated ability-effect refactoring, and the
new bodies are correctly paired:

- `ResourceReceiver.Recovery()` → `vitalStatsComponent.Recovery()` (increases current stat, clamped
  to max)
- `ResourceReceiver.Reduction()` → `vitalStatsComponent.Reduction()` (decreases current stat, clamped
  to 0)
- `RecoveryEffectDefinition.Apply()` calls `resourceReceiver.Recovery(StatType.HP, amount)` — the
  golden path a heal item pickup takes

**Not closing this bug yet**: no same-day `/code-review` ran on `1f61753` (S14-08's exact risk), no
EditMode test exists asserting the fix (as this bug's own Suggested Fix recommended), and no
owner-in-Editor Play Mode confirmation has occurred (S14-07, still unmet). Downgrade to Fixed once
one of those verifications happens; until then treat as unverified.

---

## Re-verification — 2026-09-22 (branch `main`, HEAD `d17fcc5`, merge of `e2cb75e`)

**Status changed: Apparently Fixed (unverified) → FIXED.** The polarity defect this bug was filed
for is gone, and the fix is confirmed on both sides of the call.

### Evidence — polarity is correct

`Assets/Script/Character/Player/CoreComponent/ResourceReceiver.cs:17-24`

```csharp
17:    public void Recovery(StatType statType, float amount)
19:        vitalStatsComponent.Recovery(statType, amount);

21:    public void Reduction(StatType statType, float amount)
23:        vitalStatsComponent.Reduction(statType, amount);
```

Heal maps to `Recovery`, damage maps to `Reduction`. No crossover remains.

Consumer side confirms the contract is used as intended:
`Assets/Script/System/Item/ItemEffectDefinition/RecoveryEffectDefinition.cs:13` calls
`Recovery(StatType.HP, amount)` for a healing item.

`VitalStatsComponent.Recovery()` (`VitalComponent.cs:36-47`) clamps up to
`statHandler.GetStatValue(statType)`; `Reduction()` (`:49-60`) clamps down at `0`. Both directions
behave as named.

### Two unrelated defects found in the same file — split out, NOT folded in here

Both live in `ResourceReceiver.cs` and both were previously unfiled. They are filed separately so
that closing BUG-067 does not close them by association:

| New ID | Defect |
|---|---|
| **BUG-080** | The `vitalStatsComponent` field (`:5`) is assigned only inside `ReceverModifierGroup()` (`:13`). `Recovery()` (`:19`), `Reduction()` (`:23`) and `BuffDebuffForDuration()` (`:27`) all dereference it without resolving it — NRE on the live item-pickup path. `TakeDamage()` (`:29-35`) escapes only because it resolves into a shadowing local. |
| **BUG-081** | `ResourceReceiver.TakeDamage()` (`:29-35`) is byte-identical to `NegativeReciver.TakeDamage()` (`:5-11`). Both implement `INegativeReceiver` and both components are on `Assets/Prefab/Player/PlayerTest.prefab` (verified by script GUID), so which one runs is decided by collider layout. Player-side twin of the closed BUG-053. |

**Not confirmed in Play Mode.** The polarity verdict is a static read of a three-line method and its
only consumer; no runtime test was run.
