# Bug Triage Report

> **Date**: 2026-06-20
> **Mode**: sprint (Sprint 2, 2026-06-15 to 2026-06-19 — post-sprint close)
> **Generated by**: /weekly-wrapup (automated Saturday)
> **Branch reviewed**: origin/fix-player-control (HEAD 6452127)
> **Note on parallel branch**: Commits e314b88 / 5fa5e27 / 81c95de exist on a parallel Claude session branch (not yet merged into origin/fix-player-control). They fix Bug #4 and wire combat anim events. Their status below reflects the sprint branch HEAD, not the unmerged branch.

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix this sprint (carry to Sprint 3) | 6 | Same cluster as Sprint 1 — BUG-09 added |
| P2 — Fix soon | 4 | Code-review findings from this sprint's CS diff |
| P3 — Backlog | 4 | No gameplay impact |
| P4 — Won't fix | 0 | — |

**Critical unfixed count**: 6 P1 bugs
**Fixed since last triage (2026-06-12)**: 0 on sprint branch (3 fixes on parallel branch — pending merge)
**New bugs found this triage**: 4 (P2 code-review findings)

---

## P1 Bugs — Carry to Sprint 3

| ID | System | Severity | Summary | Status |
|----|--------|----------|---------|--------|
| BUG-04 | Combat | S1 | `WeaponMelee.Attack()` foreach body empty — no `INegativeReceiver.TakeDamage()` call (`WeaponMelee.cs:29-33`) | FIXED on parallel branch e314b88 — **PENDING MERGE** to sprint branch |
| BUG-09 | Animation | S1 (Critical — "all combat states permanently stuck") | `AnimationPlayerController.OnEnable()` line 21 registers `StartAnimation` a second time (should be `EndAnimation`); `OnDisable()` line 29 mirrors the bug — unregisters `StartAnimation` twice, `EndAnimation` never unregistered. `EndAnimation` callback never fires. | UNCHANGED — `AnimationPlayerController.cs` not touched this sprint |
| BUG-06 | Game Loop | S1 | `Core.TakeDamage()` has no death check, no `ON_PLAYER_DEATH` emit; `EventID` enum missing `ON_PLAYER_DEATH` | UNCHANGED |
| BUG-05/07/08 | Enemy AI | S1 | Death chain cluster: `EntityMoveState` null-deref before guard; `EntityDeathState` wrong base class; `EntityBasicState` death block empty | UNCHANGED |
| (blocked) | Room Progression | S1 | Room-clear lock/unlock blocked on BUG-05/07/08 enemy death | UNCHANGED (blocked) |
| BUG-RC-1 | Map/Room | S1 | `RoomCell.GetDoor()` constructs `new DoorController()` (MonoBehaviour) via `new` — should return `null` | UNCHANGED |

---

## P2 Bugs — New Findings from Sprint 2 Code Review

| ID | System | File | Summary | Recommended Sprint |
|----|--------|------|---------|-------------------|
| BUG-AH-1 | AbilityHolder | `AbilityHolder.cs:4-5` | `using UnityEditor.Experimental.GraphView` + `using UnityEngine.UIElements` — Editor-only namespaces in a runtime script. **Will break Player builds.** | Sprint 3 (P1 if build is needed) |
| BUG-CORE-1 | Core | `Core.cs:24` | `GetCoreComponent<T>()` uses `coreComponents.OfType<T>().FirstOrDefault()` (LINQ) — violates `engine-code.md` "no LINQ in hot paths" and S2-03 acceptance criteria | Sprint 3 |
| BUG-PIH-1 | Input | `PlayerInputHandle.cs:269` | `Invoke(nameof(ChangeIsTakeDamage), 0.2f)` has no `CancelInvoke()` pair — rapid double-hit within 0.2s will flip `isTakeDamage` back to `false` prematurely, leaving the flag stuck `true` | Sprint 3 |
| BUG-PIH-2 | Input | `PlayerInputHandle.cs:159` | `Input.mousePosition` used inside New Input System callback — mixes legacy + new input APIs; can produce NullRef on `Camera.main` if no camera tagged | Sprint 3 |

---

## P3 Bugs — Backlog (unchanged from prior triage)

| ID | System | Summary | Disposition |
|----|--------|---------|-------------|
| BUG-RC-2 | Map/Room | `[SerializeField]` on auto-properties silently ignored | Backlog |
| BUG-DC-2 | Map/Door | `DoorController.CheckCanBeOpened()` dead method | Backlog |
| BUG-DC-3 | Map/Door | `DoorController.string Name` shadows `Object.name` | Backlog |
| BUG-RC-3 | Map/Room | `[ExecuteInEditMode]` + `Instantiate` in `Setting()` | Backlog |

---

## Code Review Findings — Sprint 2 CS Diff (28 files)

### Critical / High

| Severity | File | Finding |
|----------|------|---------|
| CRITICAL | `AbilityHolder.cs:4-5` | Editor namespace import in runtime — breaks Player builds (see BUG-AH-1) |
| CRITICAL | `AnimationPlayerController.cs:21,29` | Bug #9 UNCHANGED — `EndAnimation` never fires; all attack/skill anim exit is broken |
| HIGH | `Core.cs:24` | LINQ in `GetCoreComponent<T>()` — O(n) LINQ allocation per state entry (S2-03 incomplete, see BUG-CORE-1) |
| HIGH | `Core.cs:8` | `[SerializeField]` on auto-property `Player` silently ignored — Inspector shows nothing; use `[field: SerializeField]` or a `[SerializeField] private Player _player` backing field |

### Medium / Low

| Severity | File | Finding |
|----------|------|---------|
| MEDIUM | `PlayerState.cs:41-45` | `Enter()` calls `GetCoreComponent()` 5× per state transition with LINQ inside — O(5n) per transition; lazy-cache fields instead |
| MEDIUM | `PlayerAttackState.cs:58` | `Debug.Log("Event Call: " + statusAnimation)` — string alloc in state update, no `#if UNITY_EDITOR` guard |
| MEDIUM | `WeaponMelee.cs:104` | `Debug.Log("totalDuration" + totalDuration / 8)` — string alloc per DurationNextAttack() call |
| MEDIUM | `PlayerInputHandle.cs:269` | `Invoke()` without `CancelInvoke()` — rapid-hit timing bug (see BUG-PIH-1) |
| LOW | `WeaponMelee.cs:2` | `using Unity.VisualScripting` — unused import; remove |
| LOW | Multiple | Leftover commented-out code blocks throughout Player state hierarchy — reduces readability |

### Positive Findings

| File | What Improved |
|------|--------------|
| `CoreComponent.cs` | `Awake()` now self-registers via `core.AddCoreComponent(this)` — S2-03 self-register half done |
| `PlayerState.cs:37-50` | `Enter()` centralizes all GetCoreComponent calls instead of duplicating per-subclass |
| `PlayerInputHandle.cs:199-216` | `OnAttack()` implements combo-cancel window using `StatusAnimation` range check — architecture is correct |
| `PlayerInputHandle.cs:344-349` | `SetBufferAttack()` + `BufferIsAttack` property pattern is clean |
| `PlayerAttackState.cs:37-53` | `StatusAnimation` enum-based switch in `LogicUpdate()` replaces legacy `animFinish` bool |

---

## Parallel Branch Alert (Post-Sprint — Not Yet Merged)

Three commits on a separate Claude session branch (not on `origin/fix-player-control`) were created on Fri Jun 19:

| Commit | Summary | Impact |
|--------|---------|--------|
| e314b88 | feat(combat): complete melee combo attack system — fixes Bug #4 (WeaponMelee.Attack foreach), adds `damageMultiplier` to AttackSO, `ON_COMBO_HIT` event, `ComboCounterUI` | **Fixes P1 BUG-04** |
| 5fa5e27 | fix(combat): invoke WeaponMelee.Attack() on hit-frame anim event via PlayerAttackState.StartRangeTrigger case | Required for BUG-04 fix to fire at all |
| 81c95de | fix(combat): keep combo alive when buffered attack chains (Status = Start on chain, not None) | Combo continuity fix |

**Action required**: Review and merge these 3 commits into `origin/fix-player-control` before Sprint 3 planning. If merged, BUG-04 is CLOSED. BUG-09 remains open regardless.

---

## Trend Analysis

**Volume**: 10 open P1/P2 (−0 net vs last triage, +4 new P2 code-review bugs)
**Hot spot**: Player combat system — 6 of 10 active bugs
**Regressions**: 0 against previously completed stories
**Aged bugs (>2 sprints)**: BUG-04 (PENDING MERGE), BUG-09, BUG-06, BUG-05/07/08 — 5 bugs 3+ sprints old

> ⚠️ BUG-09 (`AnimationPlayerController` double-registration) was never assigned to a task in Sprint 2 despite being rated Critical and blocking all combat state exits. Must be the first task in Sprint 3.

---

## Recommended Actions for Sprint 3

1. **Merge the 3 parallel-branch commits** (e314b88 / 5fa5e27 / 81c95de) into `origin/fix-player-control` first — this closes BUG-04 and gives working melee combat to test from day 1.
2. **Fix BUG-09** (`AnimationPlayerController.cs:21,29`) — line 21 change `StartAnimation` to `EndAnimation`; line 29 change `StartAnimation` to `EndAnimation` in unregister. This is a 2-line fix that unblocks all combat state exits.
3. **Fix BUG-AH-1** (`AbilityHolder.cs:4-5`) — remove the two Editor namespace imports before any Player build attempt.
4. **Fix BUG-CORE-1** (`Core.cs:24`) — replace LINQ with `foreach` in `GetCoreComponent<T>()`.
5. **Fix BUG-PIH-1** (`PlayerInputHandle.cs:269`) — add `CancelInvoke(nameof(ChangeIsTakeDamage))` before the new `Invoke()` call.
