# Bug Triage Report

> **Date**: 2026-09-19
> **Mode**: sprint
> **Generated by**: `/bug-triage sprint` (autonomous, `pm-weekly-wrapup` Saturday run)
> **Sprint in scope**: sprint-14 (`production/sprints/sprint-14.md`, 2026-09-14 -> 2026-09-18, branch `sprint-14`, HEAD `17ac9af`)
> **Open bugs processed**: 13 (14 files on disk, BUG-053 closed). **4 new this cycle**: BUG-071/072/073/074, from this run's code-review pass over `d407ccc..HEAD` (46 `.cs` files, +570/-438).

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 - Fix next sprint | 5 | BUG-063, BUG-064 (item 7), BUG-065, BUG-066, BUG-073 (new) |
| P2 - Fix soon | 6 | BUG-070, BUG-071, BUG-072 (new), BUG-067 + BUG-069 (verify-only), BUG-052 |
| P3 - Backlog | 2 | BUG-068 (downgraded, dormant), BUG-074 (new) |
| P4 - Won't fix | 0 | - |

Status changes since 2026-09-13: BUG-067 **apparently fixed** (verified in source diff: `ResourceReceiver.Recovery/Reduction` now paired correctly; incidental, no test, no Play Mode). BUG-069 **apparently fixed** (`TryDoAbility` calls `CanStart()`). BUG-068 **downgraded S1 -> S3**: `GetAbility()` now has zero callers.

---

## Carried, unchanged (re-verified against source at HEAD)

| ID | Sev | Evidence |
|----|-----|----------|
| BUG-063 | S1 | `Stat.cs:63-65` still `#if UNITY_EDITOR [SerializeField]` above `modifiers`, contradicting the comment block directly above it |
| BUG-064 (item 7) | S1 | `RangeWeapon.cs:7` still `[SerializeField] private IObjecPoolService poolManager`, no `[Inject]` |
| BUG-065 | S2 | `PlayerDeathState.Enter()` only `base.Enter()`. Note also `LogicUpdate()` re-emits `ON_PLAYER_DEATH` every frame while `Status == EndRangeTrigger` |
| BUG-066 | S2 | `EntityVitalStats.cs` raw `currentStats[statType]` at lines 39,49,51,55,61,63,67 |
| BUG-070 | S2 | `VitalComponent.cs` raw `currentStats[statType]` at lines 28,39,41,45,52,54,58 |
| BUG-052 | S3 | No ADR for TD-040 (Abilities v1 vs v2) - 4th sprint of v2 work shipped |

## New this cycle

| ID | Sev | Summary |
|----|-----|---------|
| BUG-073 | S2 | `bbc2b8c` deleted `ShootObjectEffect` / `SpiritOrbProjectile`; `ShootSpirit.asset` and `SpiritProjcetile.prefab` still reference their GUIDs -> missing scripts (verified by GUID grep) |
| BUG-071 | S2 | `VitalStatsComponent.*PerTimeForDuration` builds an `IEnumerator` without `StartCoroutine` -> HoT/DoT effects are silent no-ops |
| BUG-072 | S2 (plausible) | Summon damage (`SpawnSummonEffect.SummonExecute`) reads `Services.NegativeReceiver`, never set on that path; `LightningController.Execute` is a comment-only stub |
| BUG-074 | S3 (plausible) | `StatsEffectBase` uses current value + modifiers as the amount, not the delta |

## Systemic Issues

1. **Hard-gate mitigation failed 5/5 days.** S14-02..S14-06 unchanged. The only gate items that moved (BUG-067, BUG-069) moved as side effects of off-plan work.
2. **Off-plan Abilities v2 work ships bugs every sprint** (Sprint 13: 4 bugs; Sprint 14: 4 more). Root cause is the same: no same-day review (S14-08 never decided), no QA plan (31st+ cycle), no Play Mode session (8th sprint).
3. **Deleting code without sweeping asset references** (BUG-073) repeats the BUG-064 pattern (types deleted without sweeping callers).
4. **~230 binary asset files landed in one merge** (Paladin sprites + NormalMaps). Repo-size / LFS decision pending.

## Recommendation for Sprint 15

Open with a single commit block: BUG-063, 064-7, 065, 066+070 (0.5d), plus BUG-073 (re-point or delete assets, 0.1d). No other diff until they land.
