# Bug Triage Report

> **Date**: 2026-07-10
> **Mode**: sprint (Sprint 4, in progress — `sprint-04` branch)
> **Generated by**: `/weekly-wrapup` (automated Saturday 22:00 run)
> **Branch reviewed**: `sprint-04` @ `1d9fab3` (diff since last triage `4da2c1d`, 2026-07-05)
> **CS files reviewed**: 27 files changed this week — Enemy Spawn System (EnemyManager, EnemySpawner, Database-SO/Modal/*), StatSystem (StatsSO, Stat, StatModifier, DerivedStatFormula, StatType, StatModifierTester), Core.cs, AbilityHolder.cs, AnimationPlayerController.cs, WeaponMelee.cs, WeaponMeleeStats.cs, RoomCell.cs, RoomGeneraterController.cs, LevelManager.cs, EventManager.cs, AnimationEventManager.cs, DualAbility.cs, EntityData.cs, StatsCharacter.cs, GameConstants.cs, EnemySO.cs
> **Note**: Carried bug IDs re-verified directly against current code (not assumed from last report).

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix this sprint | 3 | 4 resolved since last triage; 1 new (NRE risk in EnemySpawner) |
| P2 — Fix soon | 11 | 4 unchanged; 4 downgraded from P1/resolved import cleanups; 3 new (spawn architecture gaps + LevelManager dead condition) |
| P3 — Backlog | 6 | 1 unchanged from before, 5 re-confirmed |
| P4 — Won't fix | 0 | — |

**Critical (S1/S2) unfixed count**: 3
**Fixed since last triage (2026-07-05)**: 4 (BUG-04, BUG-09, BUG-AH-1, BUG-CORE-1 — all CLOSED, confirmed by direct code read)
**New bugs found this triage**: 5 (P1: 1 — BUG-ES-1; P2: 4 — BUG-AH-2, BUG-ES-2, BUG-ES-3, BUG-LM-1)

---

## P1 Bugs — Fix This Sprint

| ID | System | Severity | Summary | Assigned to |
|----|--------|----------|---------|-------------|
| BUG-06 | Player Damage Chain | S1 | `NegativeReciver.TakeDamage()` still `throw new NotImplementedException()` — player cannot take damage or die (`Assets/Script/Character/Player/CoreComponent/NegativeReciver.cs:8-11`) | Sprint 4 (Checklist item 6) |
| BUG-05/07/08 | Enemy AI Death Chain | S1 | Cluster unchanged: `EntityMoveState` null-deref before guard, `EntityDeathState` extends `MonoBehaviour` not `EntityState`, `EntityBasicState` death block still empty | Sprint 4 (Checklist item 7) |
| BUG-ES-1 (NEW) | Enemy Spawn | S1 | `RoomModel.GetSpawnSet()` returns `null` when `enemiesOfRoom.Count == 0` (`RoomModel.cs:29`); `EnemySpawner.GetRoomSpawnSet()` only null-checks `roomModel`, not the return value, and passes it straight through (`EnemySpawner.cs:29-37`); `SpawnRoomEnemies()` then calls `set.Count` on a possibly-null list (`EnemySpawner.cs:46`) → NullReferenceException the first time a room with an empty enemy list is loaded | Enemy-Spawn epic (S4-D3 follow-up) |

---

## Resolved Since Last Triage

| ID | System | Resolution | Verified |
|----|--------|------------|----------|
| BUG-04 | Combat | `WeaponMelee.Attack()` foreach now calls `INegativeReceiver.TakeDamage(currrentSA.attackDamege, transform.position)` — melee damage is live | `WeaponMelee.cs:26-37` read directly |
| BUG-09 | Animation | `AnimationPlayerController.OnEnable()`/`OnDisable()` now register/unregister all 5 distinct callbacks incl. `EndAnimation` — no more duplicate `StartAnimation` registration | `AnimationPlayerController.cs:15-30` read directly |
| BUG-AH-1 | AbilityHolder | `using UnityEditor.Experimental.GraphView` removed — build-breaking import gone. Note: a new unused `using UnityEngine.UIElements;` was left behind (harmless, not editor-only) — see BUG-AH-2 below | `AbilityHolder.cs:1-5` read directly |
| BUG-CORE-1 | Core | `Core.GetCoreComponent<T>()` now uses a `Dictionary` cache + `foreach` — LINQ `OfType<T>().FirstOrDefault()` is gone | `Core.cs:22-34` read directly |

---

## P2 Bugs — Active

| ID | System | File | Summary | Sprint |
|----|--------|------|---------|--------|
| BUG-CORE-2 | Core | `Core.cs:7` | `[SerializeField]` on auto-property `Player { get; private set; }` — attribute is silently ignored by Unity on auto-properties, field stays unassignable from Inspector | S4 |
| BUG-RC-2 | Map/Room | `RoomCell.cs:13,14,18-21` | Same `[SerializeField]`-on-auto-property issue, 6 occurrences | S4 |
| BUG-WM-2 | Combat | `WeaponMelee.cs:76,85` | `lastClickTime = Time.time` assigned twice in `SetAnimation()` — redundant, and the commented-out cooldown check at line 71 that would have used it is dead code | S4 |
| BUG-WM-3 | Combat | `WeaponMelee.cs:87-101` | `DurationNextAttack()` returns `0` when the current `AttackState`'s `directionAttackAnimatorOV` has no override clips — `deplayTime = 0` enables zero-cooldown attack spam | S4 |
| BUG-AH-2 (NEW) | AbilityHolder | `AbilityHolder.cs:5` | Leftover unused `using UnityEngine.UIElements;` from the BUG-AH-1 fix — not build-breaking, just dead import | S4 |
| BUG-EM-1 | Enemy Spawn | `Assets/Script/Enemy/EnemyManager.cs` | Still a bare scaffold — only `public static Instance` declared, no lifecycle (alive-count tracking, per-room reset, `ON_ENEMY_DEATH`/`ON_CLEAR_ENEMY` emission per ADR-0002). Blocks room-clear condition (Checklist item 8) | Enemy-Spawn epic |
| BUG-EM-2 | Enemy Spawn | `EnemyManager.cs:2` | `using System.Numerics;` alongside `using UnityEngine;` — no `Vector3` usage yet so no ambiguity today, but any future code using bare `Vector3`/`Vector2` in this file will hit CS0104 ambiguous-reference. Remove the unused `System.Numerics` import now before it bites | Enemy-Spawn epic |
| BUG-15 | Map/Build | `RoomGeneraterController.cs` (JSON load), `LevelManager.cs` | `File.ReadAllText(Application.dataPath + ...)` — Editor-only, room JSON won't ship in a Player build | S4 (Checklist item 14) |
| BUG-ES-2 (NEW) | Enemy Spawn | `LevelManager.cs` | `LevelManager.SpawnRoomEnemies()` duplicates `EnemySpawner.SpawnRoomEnemies()` almost verbatim — two parallel spawn drivers, **neither routes through `EnemyManager`**, contradicting ADR-0002's whole rationale (alive-count tracking, room-clear gate) | Enemy-Spawn epic |
| BUG-ES-3 (NEW) | Enemy Spawn | `Assets/Script/Manager/EventManager.cs` `EventID` enum | Missing `ON_ENEMY_DEATH` / `ON_ROOM_CLEAR` — ADR-0002's Migration Plan and Validation Criteria require both before `EnemyManager` can be built; currently nothing can signal `EnemyManager` when an enemy dies | Enemy-Spawn epic, blocks Sprint 6 |
| BUG-LM-1 (NEW) | Map/LevelManager | `LevelManager.cs:273` | `index = index == null ? 0 : index;` compares a non-nullable `int` to `null` — compiler warning CS0472, condition always false, dead logic | S4 |

---

**Resolved false alarm**: the Day-4 standup risk "`EnemySpawner.cs:57`/`RoomGeneraterController.cs:120` call `.ConvertTo<T>()` — likely CS1061 compile error" is **not a compile error**. Both files `using Unity.VisualScripting;`, which ships a `ConvertTo<T>()` extension method (package v1.9.4, pinned per `VERSION.md`) — it resolves. Still worth the cleanup suggested below (pulling a full editor-graph package in for a one-line Vector cast is unnecessary weight), but it does not block Play Mode.

---

## P3 Bugs — Backlog

| ID | System | Summary | Disposition |
|----|--------|---------|-------------|
| BUG-DC-2 | Map/Door | `DoorController.CheckCanBeOpened()` dead method, no callers | Backlog |
| BUG-DC-3 | Map/Door | `DoorController.string Name` shadows `Object.name` | Backlog |
| BUG-RC-3 | Map/Room | `[ExecuteInEditMode]` + `Instantiate` in `Setting()` | Backlog |
| BUG-17 | Map/Door | Dead no-op code: `DoorController.OpenDoor()`/`CheckCanBeOpened()`, `RoomCell.UpdateStatusDoor()` — real open/close path is `OpenDoors()`/`CloseDoor()`; recommend deleting the no-ops | Backlog |
| BUG-16 | Map/Room | `RoomType` enum not read at runtime — start/end room forced by list position `room[0]`/`room[last]`, breaks silently if `Maze_Storage.asset` is reordered | Backlog |
| BUG-12 | Architecture | `LevelManager` uses `public static Instance` singleton — violates "no new singletons" rule (not a ratified exception like `MazeController`/`EnemyManager`) | Backlog — needs refactor to Inspector ref |

---

## Systemic Issues Flagged

- **Enemy Spawn System is mid-flight and load-bearing this sprint**: `EnemyManager` is still an empty scaffold (BUG-EM-1) while `EnemySpawner`/`RoomGeneraterController`/`RoomModel` already wired a live event chain (`ON_GET_SPAWN_POSITIONS`) that calls into it. This is the classic "data pipe built before the consumer" pattern — low risk now (nothing subscribes to `EnemyManager` yet) but will block Checklist item 8 (room clear condition) until `EnemyManager` lifecycle lands. Recommend prioritizing `EnemyManager` body next.
- **`[SerializeField]` on auto-properties** (BUG-CORE-2, BUG-RC-2) is a repeating pattern across two different files/authors this sprint — worth a one-line note in `.claude/rules/scriptableobject-data.md` or `gameplay-code.md` since it's silently broken (no compile error, just doesn't serialize) and easy to miss in review.
- **No regressions** against previously completed stories detected this triage.

---

## Trend Analysis

**Volume**: 20 open bugs (P1: 3, P2: 11, P3: 6) — net +2 vs last triage (18 → 20: 4 resolved, 6 new; the Enemy Spawn System is a wholly new subsystem this week, so its bug count is additive, not a quality regression on prior code)
**Hot spot this week**: Enemy Spawn System — 5 of 20 active bugs (BUG-ES-1, BUG-ES-2, BUG-ES-3, BUG-EM-1, BUG-EM-2), all new/still-open, reflecting its scaffold status and the architecture drift flagged against ADR-0002 (spawn flow bypasses `EnemyManager` entirely)
**Regressions**: 0 against previously completed stories
**Aged bugs (>3 sprints)**: BUG-06, BUG-05/07/08 — still open since the original bug sweep; BUG-CORE-1/BUG-04/BUG-09/BUG-AH-1 finally closed this sprint after 3+ sprints open
**Good progress signal**: 4 P1 bugs closed this week (first P1 closures in several sprints) — combat damage chain (melee side) and animation end-event are now functional

---

## Recommended Actions

1. **Fix BUG-ES-1** — guard `EnemySpawner.GetRoomSpawnSet()`/`RoomModel.GetSpawnSet()` against a null return before the next Enemy-Spawn integration test; this is a live NullReferenceException waiting on any empty-enemy-list room.
2. **Build out `EnemyManager` lifecycle** (BUG-EM-1) — it is the actual blocker for Checklist item 8 (room clear condition) and is now the sprint's biggest single gap; the data pipe into it is already live.
3. **Fix BUG-06** (`NegativeReciver.TakeDamage`) — still the last piece blocking player death end-to-end; melee damage output side is now done (BUG-04 closed), only the receive side remains.
4. **Fix BUG-05/07/08 cluster** — enemy death chain, needed before `EnemyManager` can track alive-count meaningfully.
5. **Resolve BUG-ES-2** (duplicate spawn driver) — decide which of `EnemySpawner` (event-driven) or `LevelManager.SpawnRoomEnemies()` (editor-button) is canonical before both diverge further; the losing path should be deleted, not left as dead code.
6. **Add `ON_ENEMY_DEATH`/`ON_ROOM_CLEAR` to `EventID`** (BUG-ES-3) — zero-risk enum addition, unblocks Sprint 6 `EnemyManager` work whenever it starts.
7. Quick cleanup pass (low effort, high signal-to-noise): BUG-AH-2 (dead import), BUG-EM-2 (dead import, pre-empt future ambiguity), BUG-WM-2 (duplicate assignment), BUG-LM-1 (dead null-check on non-nullable int).
