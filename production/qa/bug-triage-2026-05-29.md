# Bug Triage Report

> **Date**: 2026-05-29
> **Mode**: full (no sprint file found)
> **Generated by**: /bug-triage
> **Open bugs processed**: 16
> **Sprint in scope**: N/A — No sprint plan found; assigning to backlog by demo priority

---

## Triage Summary

| Priority | Count | Notes |
|----------|-------|-------|
| P1 — Fix this sprint | 8 | All block playable demo |
| P2 — Fix soon | 5 | Block polish / full feature |
| P3 — Backlog | 3 | No gameplay impact |
| P4 — Won't fix | 0 | — |

**Critical (S1/S2) unfixed count**: 13

---

## P1 Bugs — Fix This Sprint

| ID | System | Severity | Summary | Story |
|----|--------|----------|---------|-------|
| BUG-04 | Combat | S1 | `WeaponMelee.Attack()` foreach body empty — player attacks deal 0 damage | Demo blocker |
| BUG-06 | Game Loop | S1 | No player death check in `Core.TakeDamage()` — health reaches negative, no restart | Demo blocker |
| BUG-RGC-2 | Map/Room | S1 | `RoomGridController.LoadRoom()` only handles index 0 — all other rooms throw `FileNotFoundException` | Demo blocker |
| BUG-RGC-3 | Map/Room | S1 | `swapLevelData` + `DoorPoints` never cleared between loads — wrong tiles/doors on room 2+ | Demo blocker |
| BUG-RC-1 | Map/Room | S1 | `RoomCell.GetDoor()` uses `new DoorController()` — MonoBehaviour constructed with `new`, Unity crash | Demo blocker |
| BUG-05 | Enemy AI | S2 | `EntityMoveState` dereferences `Target.transform.position` before null check (line 30) — NullRef on target loss | Demo blocker |
| BUG-07 | Enemy AI | S2 | `EntityDeathState` extends `MonoBehaviour` not `EntityState` — not wired into state machine | Demo blocker |
| BUG-08 | Enemy AI | S2 | `EntityBasicState.LogicUpdate()` Health ≤ 0 block empty — enemy never enters death state | Demo blocker |

---

## P2 Bugs — Fix Soon

| ID | System | Severity | Summary | Target |
|----|--------|----------|---------|--------|
| BUG-RGC-1 | Map/Room | S2 | `RoomGridController.OnDisable` calls `Resgister` instead of `UnResgister` — event listener leak | Before sustained play |
| BUG-RGC-4 | Map/Room | S2 | `_listTiles.Find(...).tile` — NullRef if tile name missing in list | Before content expansion |
| BUG-09 | Animation | S3 | `AnimationPlayerController.OnEnable` registers `StartAnimation` twice — `EndAnimation` never fires | Before animation polish |
| BUG-DC-1 | Map/Door | S3 | `OpenDoor()` always sets `OPEN`, never `BE_OPEN` — receiving-side door has wrong status | Before status-gated mechanic |
| BUG-RC-2 | Map/Room | S3 | `[SerializeField]` on auto-properties silently ignored — `StartDoorPosition`/`ListDirectionDoors` not in Inspector | Before debug/tuning |

---

## P3/P4 Bugs — Backlog

| ID | System | Severity | Summary | Disposition |
|----|--------|----------|---------|-------------|
| BUG-DC-2 | Map/Door | S4 | `DoorController.CheckCanBeOpened()` dead method — no body, no callers | Backlog |
| BUG-DC-3 | Map/Door | S4 | `string Name` field in `DoorController` shadows `UnityEngine.Object.name` | Backlog |
| BUG-RC-3 | Map/Room | S4 | `[ExecuteInEditMode]` + `Instantiate` in `Setting()` — duplicates door prefabs in Editor | Backlog |

---

## Systemic Issues Flagged

**Map/Room system hot spot**: BUG-RGC-1, BUG-RGC-2, BUG-RGC-3, BUG-RGC-4, BUG-RC-1, BUG-RC-2, BUG-RC-3, BUG-DC-1, BUG-DC-2, BUG-DC-3 — **10 bugs in the Map system** across the code review (2026-05-29). Signals a systemic quality issue in the room-loading rewrite; the feature needs a focused fix pass before being considered complete.

**Enemy AI cluster**: BUG-05, BUG-07, BUG-08 — 3 bugs in the EntityState death chain. All three should be fixed atomically in one PR.

---

## Trend Analysis

**Volume**: 16 open / all net new (first triage baseline)
**Hot spot**: Map/Room system — 10 of 16 bugs
**Regressions**: 0 bugs against previously-completed stories
**Aged bugs (>2 sprints old)**: BUG-04 through BUG-09 originated at project start; ~10 days unresolved at S1/S2

> ⚠️ 5 S1 bugs are currently unassigned and block the playable demo. These must be fixed before any QA session is meaningful.

---

## Recommended Actions

1. **Fix Map/Room P1 cluster** (BUG-RGC-2, BUG-RGC-3, BUG-RC-1) — rooms must render correctly before combat or AI can be evaluated. ~1.5h.
2. **Fix Combat + Game Loop** (BUG-04, BUG-06) — unblocks the full game loop. ~1h.
3. **Fix Enemy AI death chain atomically** (BUG-05, BUG-07, BUG-08) — one PR, ~30 min.
4. **Run `/sprint-plan new`** — no formal sprint tracking exists; create a sprint to sequence the P1 fixes.
