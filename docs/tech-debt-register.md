## Technical Debt Register
Last updated: **2026-09-22** (bug-documentation re-verification pass; four new entries, TD-014 re-scoped).
Previous update: **2026-09-11** (full documentation/code re-synchronisation; every entry re-checked
against `Assets/Script/` at HEAD `6d6a8e4`). Previous update: 2026-08-21.
Total items: 47 | Closed/void this pass: 0 | Corrected this pass: 1 (TD-014) | New this pass: 4 (TD-044…TD-047)

> **⚠️ Read this before using any file path below.** Commit `1c0742e` (2026-09-03) moved seven
> directories under `Assets/Script/System/`. Paths in living documents were repointed on
> 2026-09-11; paths quoted inside *historical* entries below are left as they were written.
>
> **One entry reversed direction this pass:** TD-038 was closed on 2026-08-21 and has
> **regressed** — see its row.

> **Verification note (2026-08-20).** Line numbers below were re-checked against source.
> Entries marked **CLOSED** are kept for history with the evidence that closed them; skip
> them when planning. Entries whose file no longer exists are marked **VOID**.

| ID | Category | Description | Files | Effort | Impact | Priority | Added | Sprint |
|----|----------|-------------|-------|--------|--------|----------|-------|--------|
| TD-001 | Architecture | **PARTIALLY RESOLVED 2026-08-20** — `EntityDeathState` now extends `EntityBasicState` (`EntityDeathState.cs:1`) and is constructed in `Entity.LoadState()`. Still open: `PlayerUserItemState` extends `MonoBehaviour` (`PlayerUserItemState.cs:5`) and is not in the player state machine | `PlayerUserItemState.cs:5` | S | Medium | 3 | 2026-05-31 | Backlog |
| TD-004 | Code Quality | **CLOSED 2026-08-20** — no `using UnityEditor` remains anywhere under `Assets/Script/`. The only three files that reference `UnityEditor` at all (`UIController.cs:181`, `LevelManager.cs:80,90,139`, `EntityModel.cs:10`) guard it with `#if UNITY_EDITOR` | — | S | — | CLOSED | 2026-05-31 | Closed |
| TD-008 | Code Quality | **CLOSED 2026-08-20** — `WeaponMelee.cs` was replaced by `MeleeWeapon.cs`; `OnActivate()` does `OverlapCircleNonAlloc` into a cached buffer then `INegativeReceiver.TakeDamage(currentStage.attackDamege, transform.position)`. Wired via `WeaponHolder.MakeDamage()` from `PlayerAttackState.cs:32` | `MeleeWeapon.cs:26-35` | S | — | CLOSED | 2026-05-31 | Closed |
| TD-009 | Code Quality | **CLOSED 2026-08-20** — line 21 registers `AnimationEventId.EndAnimation`, mirrored correctly at line 29. All five event ids register and unregister exactly once | `AnimationPlayerController.cs:21,29` | S | — | CLOSED | 2026-05-31 | Closed |
| TD-011 | Code Quality | **VOID 2026-09-11** — the file is gone. `EntityStatsSO.cs` was deleted in `b0512f4` as part of the entity/stat refactor (recorded as NEW-2), taking the recursive `ModifiersAmor` property with it. Enemies now use `EnemyStatSO : BaseStatsSO`. Carried as Critical/Priority-1 for 14 weeks against a file that had already been deleted | — | S | — | VOID | 2026-05-31 | Void |
| TD-012 | Code Quality | **CLOSED 2026-08-20** — `EntityMoveState` was rewritten; `LogicUpdate()` now opens with `if (!entityInput.TargetTransform)` before any dereference | `EntityMoveState.cs:24` | S | — | CLOSED | 2026-05-31 | Closed |
| TD-021 | Code Quality | **CLOSED 2026-08-20, superseded by TD-036** — `NegativeReciver.TakeDamage()` is implemented and emits `ON_PLAYER_DEATH`. The `NotImplementedException` moved to the *enemy* side: `EntityCore.TakeDamage()` (BUG-042, now TD-036) | `NegativeReciver.cs:6-14` | S | — | CLOSED | 2026-07-02 | Closed |
| TD-022 | Dependency | Room JSON loaded via `File.ReadAllText(Application.dataPath + filePath)` — Editor-only; `Assets/Data/Json/` absent from Player builds (CLAUDE Bug #15) | `RoomGeneraterController.cs:57`, `LevelManager.cs` | M | Critical | 1 | 2026-07-02 | Backlog |
| TD-005 | Code Quality | **RE-SCOPED 2026-09-11.** The `EntityWeaponMelee.Attack()` half is gone — that class was deleted (BUG-043/BUG-046 closed). Remaining scope is `EntityFindTarget`, which runs FOV + range + obstacle queries per frame per enemy now that target detection is re-enabled (NEW-1 closed), against the 0.1 ms/entity budget in `ai-code.md`. Re-measure before assuming it is still a problem | `EntityFindTarget.cs` | S | Medium | 2 | 2026-05-31 | Backlog |
| TD-006 | Code Quality | `Instantiate()` in gameplay paths without pooling. Re-scoped 2026-08-20: `Shooting.cs` is deleted, `EntityWeaponHolder`'s call is commented out, and `RangeWeapon` now pools correctly. Still unpooled: `SlashAbility.cs:35`, `LevelManager.cs:265` (Editor spawn button), `PrefabRandomItem.cs:39`, `SpawnCharacter.cs:24-32` | `SlashAbility.cs:35`, `PrefabRandomItem.cs:39`, `SpawnCharacter.cs:24` | M | Medium | 2 | 2026-05-31 | Backlog |
| TD-007 | Code Quality | **VOID 2026-08-20** — two of the three files named (`Shooting.cs`, `WeaponsController.cs`) no longer exist. Only `SlashAbility.cs:33` remains; folded into TD-006. Re-file against real files if the pattern reappears | — | S | — | VOID | 2026-05-31 | Void |
| TD-013 | Code Quality | **CLOSED 2026-08-20** — `Shooting.cs` was deleted. Ranged fire now runs through `RangeWeapon.OnActivate()`, driven by the state machine and the New Input System | — | S | — | CLOSED | 2026-05-31 | Closed |
| TD-018 | Architecture | `TalentManager.cs` hardcodes Str/Dex/Int/Cha in `Awake()` — not SO-driven. Note the class inside is named `TalentManagger` (double g) and is a plain `MonoBehaviour`, not a `CoreComponent`, despite living in `CoreComponent/`. Now overlaps `StatSystem`, which does this properly — consider deleting rather than fixing | `TalentManager.cs:6,18-26` | S | Medium | 2 | 2026-05-31 | Backlog |
| TD-025 | Architecture | `RoomType` enum never read at runtime — start/end rooms forced by list position `room[0]`/`room[last]`; reordering `Maze_Storage.asset` silently breaks selection (CLAUDE Bug #16). Line numbers corrected 2026-08-20 | `RoomGeneraterController.cs:47-48` | S | High | 2 | 2026-07-02 | Backlog |
| TD-030 | Architecture | Enemy data model split: `EnemySO` not consumed by `Entity` (which uses `EntityData`); `EnemySO` lacks a self-prefab reference — blocks the planned data-driven spawner (`EnemyManager` + `RoomModel`, formerly sketched as `EncounterSO`) | `EnemySO.cs`, `Entity.cs` | M | High | 2 | 2026-07-02 | Backlog |
| TD-031 | Architecture | Enemy-spawn prototype diverges from the 2026-07-08 GDD target. **Re-verified 2026-08-20 — roughly half of the 2026-07-13 divergence list is now out of date.** No longer true: `weight` IS clamped (`[Range(1,100)]` on `EnemyModal`); `selectionWeight` and `randomRatio` no longer exist on `RoomModel`; the uniform-random Phase-2 fill was removed; `EnemyManager` is no longer a zero-body stub. Still true: `UnityEngine.Random` (unseeded, so AC-A3/A4 stay untestable); direct `EnemyModal` refs rather than id-based lookup, no `EnemyDatabase`; `MapModel.GetRandomRoom()` shuffle-bag decouples room identity from enemy preset; two parallel runtime drivers (`EnemySpawner` event-driven + `LevelManager.SpawnRoomEnemies()` Editor button, BUG-ES-2), neither routing through `EnemyManager`; prototype sits in production `Script/`, not `prototypes/` (⚠️ corrected 2026-09-11: `prototypes/` **does** exist, created 2026-08-22). **New since:** `GetSpawnSet()` was rewritten to a candidate-pool + `RarityTier` roll — ADR-0003 Option C in shape — but its `retry > 4` fallback skips the weight filter, so the ADR's budget guarantee is broken (see TD-038); and `overflowPercent` is serialized but never read. BUG-ES-1 (BUG-033) still live in `EnemySpawner.cs:62` | `Database-SO/Modal/RoomModel.cs`, `Enemy/EnemySpawner.cs`, `LevelManager.cs` | L | High | 2 | 2026-07-09 (updated 2026-08-20) | Backlog |
| TD-023 | Architecture | `LevelManager` singleton violates the no-new-singletons rule; `RoomGeneraterController.Setting()` reaches through it for `dungeonRoomSO` / tiles / tilemaps — the code's own comment at line 34 asks for this to be refactored. Also note `Instance` is a bare mutable `public static` field, not a property, so any script can null it. The class additionally mixes editor authoring, runtime room loading and enemy spawning (SRP) | `LevelManager.cs:10`, `RoomGeneraterController.cs:34-37` | M | Medium | 2 | 2026-07-02 | Backlog |
| TD-024 | Code Quality | Dead door methods: `DoorController.OpenDoor()`/`CheckCanBeOpened()` and `RoomCell.UpdateStatusDoor()` are no-ops (door instances never DISABLE); real gating is `OpenDoors()`/`CloseDoor()` (CLAUDE Bug #17). Line numbers corrected 2026-08-20 | `DoorController.cs:29,34`, `RoomCell.cs:64` | S | Medium | 2 | 2026-07-02 | Backlog |
| TD-026 | Code Quality | `MazeController.Awake()` missing `return` after `Destroy(gameObject)` — duplicate overwrites `Instance` and re-runs generator (CLAUDE Bug #14). Note `EnemyManager.Awake()` got this right (`EnemyManager.cs:15`), so the correct shape already exists in-repo to copy | `MazeController.cs:17-20` | S | Medium | 2 | 2026-07-02 | Backlog |
| TD-029 | Code Quality | `FollowPlayer` camera: `GameObject.Find("PlayerTest(Clone)")` + `Invoke(0.01f)` timing hack — forbidden pattern; breaks on prefab rename, and the 0.01s delay races spawn order (unguarded NRE inside `Get()`). Confirmed 2026-08-20 as the **only** remaining `Find`/`FindObjectOfType` call in the codebase | `FollowPlayer.cs:11,24` | S | Medium | 2 | 2026-07-02 | Backlog |
| TD-003 | Architecture | **UPDATED 2026-09-11** — four ADRs now exist; `adr-0004` (VContainer DI) was written retroactively on 2026-09-11, 20 days after the decision shipped in `aa4e620`. Still open: `adr-0001` and `adr-0002` remain `Status: Proposed` after 9+ weeks (2 of 4 ADRs unratified), and the no-ADR list has **grown**, not shrunk — `Character/Base/`, `Pathfinding/`, `PoolableService/`, plus the new Item system, Abilities v2, and the UI layer (BUG-052). Separately, **no ADR decides the fate of the two coexisting ability frameworks** | `docs/architecture/` | L | High | 3 | 2026-05-31 | Ongoing |
| TD-014 | Test Debt | 0 test files (EditMode or PlayMode) — no regression safety net for any system. ⚠️ **Re-scoped 2026-09-22: this is currently impossible, not merely unstarted.** See **TD-044** / BUG-084 — there is no `.asmdef` anywhere under `Assets/`, and `tests/` sits outside the Unity asset tree, so Test Runner can neither compile nor discover a test. TD-044 must be done first; every 0.3d "write the first EditMode test" estimate in sprints 14 and 15 is mis-scoped until then | All | XL | High | 3 | 2026-05-31 | Ongoing (blocked by TD-044) |
| TD-016 | Documentation | **RE-SCOPED 2026-08-20 — the original description was misleading.** `AnimationName.cs` is not an empty constants file; it is an unrelated empty `ScriptableObject` stub with a `[CreateAssetMenu]` attribute. The real constants live in `GameConstants.AnimationName` and *are* used (e.g. `EntityBasicState.cs:27`). Two things actually remain: delete the dead SO stub, and add the `SKILL` and `DEATH` constants that `GameConstants` is still missing | `AnimationName.cs:5-9`, `GameConstants.cs:9-20` | S | Low | 4 | 2026-05-31 | Backlog |
| TD-017 | Documentation | `UIManager.cs` is still an empty stub (`Start`/`Update` empty). Context added 2026-08-20: a stats panel (`UI/StatsUIController.cs`, `StatSlot.cs`) and UI Toolkit menus (`UI/UIController.cs`) now exist and cover part of the HUD need, but neither has a GDD and health/mana are still displayed nowhere | `Manager/UI/UIManager.cs` | M | Medium | 3 | 2026-05-31 | Backlog |
| TD-028 | Code Quality | Duplicated room-loading logic: `LevelManager.LoadRoom()` and `RoomGeneraterController.LoadRoom()` are parallel implementations, both marked `//Refactor late`. Line numbers corrected 2026-08-20 | `LevelManager.cs:270-312`, `RoomGeneraterController.cs:59-137` | M | Medium | 3 | 2026-07-02 | Backlog |
| TD-033 | Architecture | **CLOSED 2026-08-20** — `Assets/Script/Pooling/ObjectPooling.cs` was deleted and superseded by the generic `Assets/Script/System/PoolableService/` pool (`ObjectPoolManager`, `Pool`, `PoolMember`, `IPoolable`), now consumed by `RangeWeapon`, `EnemySpawner` and `StatsUIController`. Resolves the BUG-052 reconciliation request. ⚠️ `Poolable/` itself still has no ADR and no GDD | `Assets/Script/System/PoolableService/` | M | — | CLOSED | 2026-07-02 | Closed |
| TD-019 | Architecture | `EntityData.rangeCheckChase` field still missing. Corrected 2026-08-20: after the `EntityMoveState` rewrite the literal `10` at line 27 is a **no-target timeout in seconds**, not a chase range — so the hardcoded value is now a magic timer, and the missing chase-range field is a separate gap | `EntityMoveState.cs:27`, `EntityData.cs` | S | Low | 4 | 2026-05-31 | Backlog |
| TD-002 | Architecture | **CLOSED 2026-08-20** — `PlayerCombat.cs` no longer exists on disk | — | S | — | CLOSED | 2026-05-31 | Closed |
| TD-020 | Architecture | Legacy files not removed: `Map/Legacy/Door.cs`, `Map/Legacy/Room.cs` — superseded, still in project | `Assets/Script/Map/Legacy/` | S | Low | 4 | 2026-05-31 | Backlog |
| TD-027 | Code Quality | `BaseGrid.GetNext()` has no bounds check — negative/wrapping index → latent IndexOutOfRangeException (masked: edge cells never carve outward passages). Re-verified 2026-08-20, unchanged | `BaseGrid.cs:34-41` | S | Low | 4 | 2026-07-02 | Backlog |
| TD-032 | Code Quality | `RoomGeneraterController.Setting()` appends to the `Maze_Load_Room` SO asset without clearing first; relies solely on `OnDisable` cleanup — room list grows if a play session is interrupted. Line numbers corrected 2026-08-20 | `RoomGeneraterController.cs:27,45` | S | Low | 4 | 2026-07-02 | Backlog |
| TD-015 | Documentation | **MERGED into TD-003 on 2026-08-20** — duplicate entry, no independent tracking value | — | — | — | VOID | 2026-05-31 | Void |
| TD-010 | Code Quality | Intentional typos preserved for serialization compatibility: `attackDamege`, `currrentSA`, `deplayTime`, `CaculateIndex`, `uppgradeData`, `Resgister` | Multiple | S | Low | 5 | 2026-05-31 | Accepted |
| TD-034 | Performance | Enemy-count scaling package for wave rooms (~15 typical, 20 hard ceiling, midcore) — deferred while demo enemy counts stay low: (1) active-attacker throttle in `EnemyManager` (`maxConcurrentAttackers` 3–4) gating the `EntityBasicState` attack transition — combat-fairness lever, depends on re-enabling `base.LogicUpdate()` in `EntityMoveState` (this cited "CLAUDE Bug #3", which is SUPERSEDED — the intended reference is Bug #5; corrected 2026-08-20); (2) `PathRequestManager.maxRequestsPerFrame` 2→3–4; (3) local avoidance / separation in `EntityMovement` (NonAlloc OverlapCircle, radius/weight in `EntityData`); (4) pool A* buffers (PriorityQueue/HashSet/List) in `PathRequestManager`; (5) LOS-skip in `EntityMovement.ChaseToTarget`; (6) kinematic `Rigidbody2D` on enemy prefabs; (7) wave-room `LENGTH_ROOM` 10→12 (~36u). WHY accepted: no gameplay/perf impact at current low counts; revisit when wave rooms actually target 15–20/room. | `EnemyManager.cs`, `EntityBasicState.cs`, `EntityMovement.cs`, `PathRequestManager.cs`, `AStar.cs`, `GameConstants.cs` | L | Medium | 3 | 2026-07-29 | Backlog |
| TD-035 | Performance | Structural horde-pathfinding rewrites for >30 enemies/room — deliberately NOT needed at ≤20 (over-engineering below ~30/room): generation-stamp replacing `PathfindingGrid.ClearSearchState` O(N) scan; closed-set flag on `SearchNode` (drop per-request HashSet); distance-priority request queue; distance-based repath LOD + jitter; shared flow-field / Dijkstra-from-player (O(1) per enemy) as the endgame for true hordes. WHY accepted: current A* + TD-034 package covers up to ~20/room; revisit only if the roadmap targets horde spectacle (30–50+). | `AStar.cs`, `PathfindingGrid.cs`, `SearchNode.cs`, `PathRequestManager.cs`, `EntityMovement.cs` | XL | Low | 4 | 2026-07-29 | Backlog |
| TD-036 | Architecture | **CLOSED 2026-09-11** — every sub-item verified fixed against HEAD `6d6a8e4`: `EntityCore.TakeDamage()` removed (BUG-042), `EntityNegativeReciver` rewritten to apply `StatType.Defense` and write `EntityVitalStats` with no player logic (BUG-053), `EntityStatsSO.cs` and `EntityWeaponMelee.cs` deleted. Enemies can die. ⚠️ Two residuals were re-filed rather than left inside this entry: TD-041 (`EntityVitalStats` key guard, = BUG-066) and TD-042 (`EntityAttack` duplicate, = BUG-043). Original text follows. **NEW 2026-08-20.** Enemy health has two disconnected stores and two `INegativeReceiver` implementers. Damage lands on `EntityNegativeReciver.currentHealth` (a plain `public int`), while the death check in `EntityBasicState.LogicUpdate()` reads `entity.Data.StatsSO.Health`, which nothing decrements — so enemies can never die. `EntityCore.TakeDamage()` throws `NotImplementedException` (BUG-042), and `EntityNegativeReciver` additionally resolves `PlayerInputHandler` off an `EntityCore` (→ NRE) and emits `ON_PLAYER_DEATH` on an enemy death (BUG-053). Violates `gameplay-code.md` ("health changes must go through the Core hub") and single-source-of-truth. Fix per story S10-01: implement `EntityCore.TakeDamage()`, route through `EntityStatsSO`, delete `EntityNegativeReciver.cs` | `EntityCore.cs:9-12`, `EntityNegativeReciver.cs:5-14`, `EntityBasicState.cs:30` | M | Critical | 1 | 2026-08-20 | Sprint 10 |
| TD-037 | Code Quality | **NEW 2026-08-20.** `EntityInput.Update()` has `//GetTargetInRange();` commented out — the only writer of `targetTransform`. `TargetTransform` is therefore always `null`, so enemies never detect the player, `EntityMoveState` always takes the no-target branch, and `EntityAttackState.cs:24` would NullRef the moment it is reached. Nothing in enemy AI works until this is restored. Restore with the null guard `ai-code.md` requires **and** NonAlloc queries, otherwise it reintroduces two allocating `OverlapCircle` calls per enemy per frame (TD-005) | `EntityInput.cs:67` | S | Critical | 1 | 2026-08-20 | Sprint 10 |
| TD-038 | Data | **⚠️ REOPENED 2026-09-11 — REGRESSION.** `Stat.cs:63-65` now reads `#if UNITY_EDITOR` / `[SerializeField]` / `#endif` directly above `modifiers`, undoing the fix. The guard does not help: Unity serializes assets *in the Editor*, which is exactly where the original leak occurred. Tracked as **BUG-063**, open with a documented one-line fix and no technical blocker, carried 24+ consecutive triage cycles — the project's most persistent open item. The extensive "NEVER add [SerializeField] to this field" comment directly above it did not prevent the change; a comment was not a sufficient guard. Previous (now invalid) resolution follows. **CLOSED 2026-08-21.** `Stat.modifiers` carried `[SerializeField]` against its own doc-comment and ADR-0001, so runtime buffs were written into `StatsSO` assets — two `STR +1 Flat` modifiers reached `PlayerStats.asset` and `Test.asset` and were committed to git. Assets cleaned on `sprint-10`; the attribute removed on 2026-08-21 (owner decision C1, option 3 — bare private field, with a warning comment as the guard since that state is otherwise silent). ⚠️ Not to be confused with `StatModifierGroup.authoredModifiers` on `WeaponStats`, which **must stay** serialized. The `\|\|`-instead-of-`&&` skip-guard in `StatsSO.RecalculateDerived()` was separately fixed on `sprint-10`. The `RoomModel` half of the original entry moved to TD-039 | `Stat.cs:49-62` | S | — | CLOSED | 2026-08-20 | Closed |
| TD-039 | Architecture | **NEW 2026-08-21 — ACCEPTED, not scheduled** (owner decision C3: docs follow code for now). `RoomModel.GetSpawnSet()` deviates from ADR-0003 in three ways, all documented in that ADR's 2026-08-21 amendment. (1) **Budget invariant broken** — the `retry > 4` fallback (`RoomModel.cs:55-58`) does `AddRange(enemiesOfRoom)` with no weight filter and no rarity roll, so `totalSpend` can exceed `weightBudget`; real bound is `B + max(weight)`. Measured over 20 000 simulated rooms at B=100: ~4% of rooms overspend with a mixed pool, worst case **+80 (+80% of budget)** — rare but severe, and silent (no log). (2) **Not a pure method** — it mutates the serialized `candidateEnemies` field as scratch, so it dirties the asset during play, is not re-entrant, and is unsafe if two rooms share a `RoomModel` (which `MapModel.GetRandomRoom()`'s shuffle-bag permits). `[NonSerialized]` on that field fixes the churn. (3) **`overflowPercent` is dead** — serialized, `[Range(0,1)]`, never read; `RoomModel.cs:31` hardcodes `0.1f`. Revisit trigger: playtest feedback about unfairly hard rooms — this is the first suspect. Wiring `overflowPercent` into the fallback would fix (1) and (3) together | `RoomModel.cs:31,55-58` | S | Medium | 4 | 2026-08-21 | Accepted |
| TD-040 | Architecture | **NEW 2026-09-11.** Two ability frameworks run side by side with no ADR choosing between them. v1 (`System/Skill_Ability/`, `ActivateSkill` subclasses) drives `WeaponStats.AbilityWeapon`/`.SkillWeapon`, `AttackSO.ability`, `Weapon` and `EntityWeapon`. v2 (`System/Abilities/`, `AbilityDefinition` composition) drives the player through `AbilityHolder`. Both have live SO assets. New contributors have no way to know which to author against; `design/gdd/skill-ability-system.md` documents only v1 | `System/Skill_Ability/`, `System/Abilities/` | L | High | 2 | 2026-09-11 | Backlog |
| TD-041 | Code Quality | **MERGED 2026-09-25 (still open).** The ADR-0005 refactor moved the unguarded dictionary into the shared `VitalStatsBase.cs:28,39,41,45,52,54,58`, which both `EntityVitalStats` and `VitalStatsComponent` derive from. This entry and the player-side instance (BUG-070) are now literally the same seven lines — one guard closes both. `:28` is on the live death path. **Original text follows.** **NEW 2026-09-11 (= BUG-066).** `EntityVitalStats` indexes `currentStats[statType]` with no key-existence guard. Any `StatType` absent from an entity's stat profile throws `KeyNotFoundException` on the live damage chain. `ai-code.md` and `gameplay-code.md` both require this guard | `EntityVitalStats.cs` | S | High | 2 | 2026-09-11 | Sprint next |
| TD-042 | Architecture | **CLOSED 2026-09-25.** `EntityAttack.cs` was deleted outright in `f7d98b1` ("no weapon, no attack"). The hardcoded `TakeDamage(10, …)` is gone with it, and an enemy now attacks only through `EntityWeaponHolder` + a `Weapon` prefab pointed at by `EntityData.WeaponSO`. Closes the BUG-043 residual. **Original text follows.** **NEW 2026-09-11 (= BUG-043 residual).** `EntityAttack.Attack()` survives as a second enemy attack path alongside `EntityWeapon`, and hardcodes `TakeDamage(10, …)` — a magic number in gameplay code, which `gameplay-code.md` forbids outright. The worse duplicate (`EntityWeaponMelee`) was deleted; this one was not | `EntityAttack.cs:33` | M | Medium | 2 | 2026-09-11 | Closed |
| TD-043 | Code Quality | **NEW 2026-09-11.** Orphan `.meta`-only directories left by reverted or moved work, invisible in a file listing but present in git: `Assets/Script/Character/Boss/` (3 `.meta`, from the boss revert `4421fdc`), `Assets/Script/Handler/` (1 `.meta`), `prototypes/skill-enhance-abilities/Scripts/` (2 `.meta`, from the Abilities v2 promotion). Harmless, but they make the tree misleading | multiple | S | Low | 4 | 2026-09-11 | Backlog |
| TD-044 | Test Debt | **NEW 2026-09-22 (= BUG-084).** The precondition of TD-014, not a symptom of it. `find Assets -name "*.asmdef"` returns **0**, so no assembly can reference `UnityEngine.TestRunner` / `UnityEditor.TestRunner` and Unity Test Runner cannot discover a test. Independently, `tests/EditMode` and `tests/PlayMode` are siblings of `Assets/`, so Unity never compiles them. The root `GameRPG.Combat.EditModeTests.csproj` is an untracked stale IDE artifact citing an `Assets/Tests/` directory that does not exist. TD-014 has been carried XL/Ongoing since 2026-05-31 and sprint-14 S14-13 / sprint-15 estimate "write the first EditMode test" at 0.3d — that estimate is wrong; the task cannot start. Note the runtime `.asmdef` is itself a breaking change: it splits `Assembly-CSharp` and surfaces every implicit cross-directory dependency at once | `Assets/` (no asmdef), `tests/`, `.claude/rules/test-standards.md` | M | Critical | 1 | 2026-09-22 | Backlog |
| TD-045 | Architecture | **SCOPE REDUCED 2026-09-25 (still open).** The player now inherits `Reborn()` from the shared `VitalStatsBase.cs:20-24`, so the reset mechanism this entry said was missing exists. What remains: nothing calls it after `Start()`, and there is still no owner — `ON_PLAYER_DEATH` has 0 subscribers, `class GameManager` has 0 hits, `ON_REALOAD_GAME` has 0 emitters and 0 subscribers, all re-verified at HEAD `c0067f4`. Death is still a permanent hard lock. Fix BUG-086 (the event fires every frame) before adding the first subscriber. **Original text follows.** **NEW 2026-09-22 (= BUG-087).** Player death has no recovery path and no owner. `ON_PLAYER_DEATH` has zero subscribers; `grep GameManager Assets --include=*.cs` = 0 hits; player-side `VitalStatsComponent` has no `Reborn()` and fills `currentStats` once in `Start()`; `ON_REALOAD_GAME` has 0 emitters and 0 subscribers; `PlayerData.Reborn()` still has no caller. Death is a permanent hard lock — the "death/restart" third of the demo life cycle is unimplemented. The enemy side already has the pattern: `EntityVitalStats.Reborn()` called from both `Start()` (`:23`) and `OnEnable()` (`:28`). This is the unresolved half of the long-standing Bug #6 / S10-08, promoted out of that entry because it is a feature, not a fix | `PlayerDeathState.cs:18`, `VitalComponent.cs:19-24`, `EventManager.cs:42-43`, `PlayerData.cs` | M | Critical | 1 | 2026-09-22 | Backlog |
| TD-046 | Architecture | **NEW 2026-09-22, ⚠️ CORRECTED SAME DAY after owner review — the architectural claim is WITHDRAWN.** As filed this entry said the `Action<AbilityContext>` spawn callback cannot carry a hit target, forcing per-hit state through a shared mutable field, and called that a design flaw. It is not. The set-then-invoke pattern is intentional and is already implemented correctly at `SpawnProjectileBase.cs:31-33` (assign `Services.NegativeReceiver`, then `_callback.Invoke`) — the two statements are adjacent and synchronous, so there is no interleaving hazard. The summon side has the same wiring in place (`SpawnSummonBase.cs:12-15` invokes the callback from a Unity Animation Event, and `LightningController.cs:18` calls `base.Execute()`). **What is actually missing is one implementation, not a redesign**: `LightningController.Execute()` (`:12-19`) still has its overlap query as three comment lines, so it never assigns a receiver before invoking. Tracked as BUG-072 at its original scope. Retained here only as the record of a withdrawn claim — **do not action this entry**; action BUG-072 | `LightningController.cs:12-19` | S | Low | WITHDRAWN | 2026-09-22 | Void |
| TD-047 | Architecture | **CLOSED 2026-09-25.** The ADR-0005 refactor collapsed both implementers into one abstract base, `NegativeReceiverBase<TCore>` (`Assets/Script/Character/Base/NegativeReceiverBase.cs:7`), and `ResourceReceiver` dropped `INegativeReceiver` entirely — it is now `: Interact` with an empty body. `PlayerTest.prefab` still carries both components, which is correct: one is the hurtbox receiver, the other the pickup interactor, and they no longer answer the same call. The secondary item is closed too: both `EnemyPrefab.prefab` files carry **0** references to the player `NegativeReciver` GUID. **Original text follows.** **NEW 2026-09-22 (= BUG-081).** Two `INegativeReceiver` implementers on the player with byte-identical `TakeDamage()` bodies — `NegativeReciver.cs:5-11` and `ResourceReceiver.cs:29-35` — and **both components are on `Assets/Prefab/Player/PlayerTest.prefab`** (verified by script GUID). Which one runs is decided by collider layout, not by design. Identical today, so behaviour is unaffected; the trap is that the next edit to one silently creates two different damage behaviours on the same character. Direct repeat of BUG-053's shape, which shipped broken on the enemy side for six sprints. `.claude/rules/ai-code.md` states the one-implementer rule for enemies; no equivalent rule exists for the player. Secondary: `Assets/SO/Database/EnemyPrefab.prefab` carries the *player* `NegativeReciver` (a `CoreComponent<Core>`) and no `EntityNegativeReciver` | `NegativeReciver.cs:5`, `ResourceReceiver.cs:29`, `PlayerTest.prefab`, `EnemyPrefab.prefab` | S | High | 2 | 2026-09-22 | Closed |
| TD-048 | Process | **NEW 2026-09-22 (root cause of BUG-088).** Nothing in this project compiles the code except a human opening the Unity Editor. Commit `723fab1` pushed `EntityMovement.SetPositionToCheck()` containing `Random.range` (the member is `Random.Range`) and `trasnform.postion` (two typos naming nothing) — CS0117 and CS0103, so `Assembly-CSharp` does not build and **every** other script in the project is unavailable. It reached a branch tip and sat there. There is no CI, no pre-push hook that compiles (TD-009's hook is a placeholder, carried 25+ cycles), and no `.asmdef` (TD-044) that would let a headless `Unity -batchmode -quit -projectPath . -executeMethod` compile check be scoped or fast. A compile gate is the cheapest possible test and the project has none — this is why a two-typo commit can block an entire sprint's verification. Minimum viable fix: a pre-push hook invoking Unity in batch mode and failing on any CS error. Depends on nothing; does **not** depend on TD-044 | `.githooks/`, CI (none), `EntityMovement.cs:163-164` | S | Critical | 1 | 2026-09-22 | Backlog |
| TD-049 | Code Quality | **CLOSED 2026-09-25.** All three loose ends resolved. (1) BUG-089 was closed by owner review on 2026-09-22 — the unconditional `Apply()` is correct under the upgrade-tier design, and `CancelHold()` is a deliberate force-release; reopen at demo/release phase. (2) BUG-091 fixed and committed in `379c267` — null-element guards at `AbilityDefinition.cs:58,63`, and all three lists carry `= new()`. (3) `CheckPayCostValid()` on `AbilityDefinition` deleted. `c0067f4` additionally gave `AbilityEffectDefinition.SubConditions` / `.SubEffects` / `.Costs` initialisers, closing the last residual of BUG-082. **Original text follows.** **NEW 2026-09-22 (= BUG-089 + BUG-091, from commit `2a83469`).** The gate consolidation in `2a83469` is architecturally right — one condition+cost check in `AbilityDefinition.TryStart()`, reached through `CanStart()`, replacing three scattered walks — and it closed BUG-076 and BUG-077. Three loose ends came with it, all in the same two files. (1) A refused `TryCast()` calls `CancelHold()` and nothing else, so `CastInstant():54` still advances to `Do` and `Execute()` applies the refused effect anyway: the gate refuses payment but not the effect (BUG-089). (2) `TryStart()` dropped the `if (condition == null) continue` guard the deleted `ValidateConditions()` had, and its `&&` early-return lets a null `Costs` or `Conditions` reach a `foreach` (BUG-091). (3) `AbilityDefinition.CheckPayCostValid()` was added with zero callers and reproduces BUG-082's null-check-after-dereference verbatim, so that defect now exists in two files. WHY grouped: one commit, one author intent, one review pass closes all three | `AbilityInstance.cs:68-82`, `AbilityDefinition.cs:50-75` | S | High | 2 | 2026-09-22 | Closed |
| TD-050 | Code Quality | **NEW 2026-09-25.** `b0b1337` renamed `Assets/Script/Character/Base/DamageReceiverBase.cs` to `NegativeReceiverBase.cs` but did not rename the `.meta`. `DamageReceiverBase.cs.meta` is orphaned and `NegativeReceiverBase.cs` has no `.meta` at all, so Unity will mint a new GUID for it on the next import. Harmless today — the class is abstract, so no GameObject references it, and the orphan GUID `8c1a7c078b91417ba4bfdf0c016f48ae` is referenced by no prefab, asset or scene (verified by grep over `Assets/`). Fix: delete the orphan `.meta` and let Unity generate the new one. WHY worth a row: the same slip on a *concrete* MonoBehaviour would silently detach every component instance in every prefab and scene, which is exactly the failure mode of BUG-073 and BUG-090 | `Assets/Script/Character/Base/DamageReceiverBase.cs.meta`, `NegativeReceiverBase.cs` | S | Low | 4 | 2026-09-25 | Backlog |
| TD-051 | Process | **NEW 2026-09-25 (= BUG-092, second occurrence of TD-048).** A second compile break was pushed to this branch four days after the first. `c0067f4` deleted the field `public float perTime;` from `RecoveryReductionPerTimeForDuration.cs` and left both call sites (`:14`, `:17`) reading it — `error CS0103: The name perTime does not exist in the current context`. Same class of failure as BUG-088 (`723fab1`), same consequence: `Assembly-CSharp` does not build, so no Play Mode, no Test Runner, and no bug in the register can be verified in the Editor. This is not a new debt item so much as evidence that **TD-048 is now the highest-value open process item**: two blocking breaks, zero detection, in one working week. Do not merge this row into TD-048 — it is the recurrence record that justifies scheduling it | `RecoveryReductionPerTimeForDuration.cs:14,17`, `.githooks/`, CI (none) | S | Critical | 1 | 2026-09-25 | Backlog |
